# Tasks

- [x] Task 1: 起点（拉取主分支并核对工作树）
  - [x] SubTask 1.1: `git fetch origin && git status`，确认本地 `main` 与 `origin/main` 同步
  - [x] SubTask 1.2: `git checkout main`（如不在 main），确保站在主分支
  - [x] SubTask 1.3: `git log --oneline -5` 核对最近提交，确认未回滚任何用户改动

- [x] Task 2: 编译基线（确认起点干净）
  - [x] SubTask 2.1: `cargo build --release --features persist` 退出码 0 且无 warning
  - [x] SubTask 2.2: `cargo build --release`（默认 feature）退出码 0 且无 warning
  - [x] SubTask 2.3: `cargo test --release --lib --features persist` 全部通过（43/43）
  - [x] SubTask 2.4: `cargo test --release --lib`（默认 feature）全部通过（43/43）

- [x] Task 3: 部署扩展到 PHP extensions 目录
  - [x] SubTask 3.1: `cp target/release/libxhjob.so $(php-config --extension-dir)/xhjob.so`
  - [x] SubTask 3.2: `php -d extension=xhjob.so -m | grep xhjob` 确认加载（输出 xhjob）

- [x] Task 4: 评估并启用 dead code 真实功能（不删除有未来价值的代码）
  - [x] SubTask 4.1: 启用 `is_retryable_http_status` / `is_retryable_shell_exit`：修改 `RetryPolicy::should_retry` 签名为 `should_retry(&self, task: &Task, result: &TaskResult) -> bool`，按 task_type 真实调用判断函数
  - [x] SubTask 4.2: 同步修改 `src/scheduler/queue.rs::process_one` 中调用 `schedule_retry` 的位置，先用 should_retry 判断，不可重试直接 FAILED
  - [x] SubTask 4.3: 删除 `sleep_for_retry` 函数（无启用路径）
  - [x] SubTask 4.4: `Event` 结构体加 `#[allow(dead_code)]` + doc comment 说明未来 IPC 事件流
  - [x] SubTask 4.5: `should_fire_missed` 加 `#[allow(dead_code)]` + doc comment 说明未来 cron misfire 策略
  - [x] SubTask 4.6: `make_store` / `default_db_path` 加 `#[allow(dead_code)]` + doc comment 说明 store 工厂
  - [x] SubTask 4.7: `thread_pool` 模块加 `#[allow(dead_code)]` + 模块 doc comment 说明未来 CPU 密集任务

- [x] Task 5: 简化 `next_fire` 签名（删除被忽略的 `seconds` 参数）
  - [x] SubTask 5.1: `next_fire` 函数签名删除 `seconds: bool` 参数与 `let _ = seconds;`
  - [x] SubTask 5.2: `scan_once` 中两处 `next_fire` 调用同步删除 `false`
  - [x] SubTask 5.3: `TaskBuilder::build` 中 `next_fire` 调用同步删除 `false`
  - [x] SubTask 5.4: 单元测试中所有 `next_fire` 调用同步删除 `false`（6 处）

- [x] Task 6: cron 表达式非法时 dispatch 立即失败
  - [x] SubTask 6.1: `build()` cron 解析失败返回 `Err(XhjobError::CronParse(...))`（非 warn 后继续）
  - [x] SubTask 6.2: `dispatch()` 失败返回 `error: cron parse: ...` 字符串（PHP 端，已验证路径）

- [x] Task 7: 新功能 — cron 执行次数限制 `maxExecutions(N)`
  - [x] SubTask 7.1: `Task` struct 新增 `max_executions: u32` 与 `execution_count: u32`（带 `#[serde(default)]`）
  - [x] SubTask 7.2: `Task::new` 初始化为 0
  - [x] SubTask 7.3: `TaskBuilder` 新增 `max_executions: u32` + builder 方法 + `build()` 复制
  - [x] SubTask 7.4: `Xhjob` 类新增 `max_executions(&mut self, n: i64)`，PHP 暴露为 `maxExecutions`
  - [x] SubTask 7.5: `scan_once` 中跳过 `max_executions > 0 && execution_count >= max_executions` 的任务
  - [x] SubTask 7.6: `process_one` 中执行成功后递增 `execution_count` 并检查上限
  - [x] SubTask 7.7: `TaskStore` trait 新增 `increment_execution_count(&self, id: &str) -> Result<u32>`
  - [x] SubTask 7.8: `in_memory.rs` 实现
  - [x] SubTask 7.9: `sqlite.rs` schema 新增两列 + ALTER TABLE 兼容 + `task_from_row` / `insert_task` / `increment_execution_count` 实现
  - [x] SubTask 7.10: `xhjob_state` 返回新增 `execution_count` 与 `max_executions` 字段
  - [x] SubTask 7.11: `StateInfo` 新增两字段 + `from_task` 赋值

- [x] Task 8: 新功能 — 任务暂停/恢复/取消/删除（对齐 APScheduler pause/resume/remove + Celery revoke）
  - [x] SubTask 8.1: `TaskState` 新增 `Cancelled` 终态 + `as_str`/`from_str`/`is_terminal` 同步
  - [x] SubTask 8.2: `Task` struct 新增 `paused: bool` 与 `cancel_requested: bool`（带 `#[serde(default)]`）
  - [x] SubTask 8.3: `TaskStore` trait 新增 `remove_task` / `set_paused` / `cancel_task`
  - [x] SubTask 8.4: `in_memory.rs` 与 `sqlite.rs` 实现新方法（SQLite schema 新增 `paused`/`cancel_requested` 列 + 旧库 ALTER TABLE 兼容）
  - [x] SubTask 8.5: `scan_once` 跳过 `paused || cancel_requested` 的任务
  - [x] SubTask 8.6: `process_one` 中执行结束后 reload task 检查 `cancel_requested`，若为 true 置 Cancelled 终态并跳过重试
  - [x] SubTask 8.7: IPC 协议（字符串 op 风格）已支持新命令，无需新增枚举
  - [x] SubTask 8.8: `daemon_main.rs` 新增 4 个 handler：`handle_remove_op` / `handle_pause_op`（pause/resume 共用）/ `handle_cancel_op`
  - [x] SubTask 8.9: `lib.rs` 新增 4 个 PHP 顶层函数 `xhjob_remove` / `xhjob_pause` / `xhjob_resume` / `xhjob_cancel`
  - [x] SubTask 8.10: `StateInfo` 新增 `paused: bool` + `xhjob_state` 返回 `paused`

- [x] Task 9: 新功能 — 任务起始/结束时间窗口（对齐 APScheduler start_date/end_date）
  - [x] SubTask 9.1: `Task` struct 新增 `start_date: Option<i64>` 与 `end_date: Option<i64>`（带 `#[serde(default)]`）
  - [x] SubTask 9.2: `TaskBuilder` 新增 `start_at(ts)` / `end_at(ts)` builder 方法
  - [x] SubTask 9.3: `Xhjob` 类新增 `start_at` / `end_at`，PHP 暴露为 `startAt` / `endAt`
  - [x] SubTask 9.4: `scan_once` 中检查时间窗口：now < start_date 跳过；now > end_date 置 Success 终态
  - [x] SubTask 9.5: `sqlite.rs` schema 新增 `start_date` / `end_date` 两列 + ALTER TABLE 兼容
  - [x] SubTask 9.6: `StateInfo` 新增两字段 + `xhjob_state` 返回 `start_date` / `end_date`

- [x] Task 10: 新功能 — 任务列表查询（对齐 APScheduler get_jobs）
  - [x] SubTask 10.1: `TaskSummary` struct 定义 + `impl From<&Task>` + `TaskStore::list_tasks(state_filter)` trait 方法
  - [x] SubTask 10.2: `in_memory.rs`（filter + sort by created_at）与 `sqlite.rs`（SELECT + 可选 WHERE state=?）实现
  - [x] SubTask 10.3: IPC 字符串命令 `list` 路由（无需新增枚举）
  - [x] SubTask 10.4: `daemon_main.rs` 新增 `handle_list_op`（state_filter 字符串映射到 TaskState）
  - [x] SubTask 10.5: `lib.rs` 新增 `xhjob_list` 返回 JSON 字符串（PHP 端 json_decode）

- [x] Task 11: 新功能 — 任务结果过期清理（对齐 Celery result_expires）
  - [x] SubTask 11.1: `Task` struct 新增 `result_ttl: u64`（默认 0=永久）
  - [x] SubTask 11.2: `TaskBuilder` 新增 `result_ttl(secs)` builder 方法
  - [x] SubTask 11.3: `Xhjob` 类新增 `result_ttl`，PHP 暴露为 `resultTtl`
  - [x] SubTask 11.4: `TaskStore` trait 新增 `cleanup_expired_results() -> Result<u64>`
  - [x] SubTask 11.5: `in_memory.rs` 与 `sqlite.rs` 实现新方法
  - [x] SubTask 11.6: `sqlite.rs` schema 新增 `result_ttl` 列 + ALTER TABLE 兼容
  - [x] SubTask 11.7: `scan_once` 周期性调用 `cleanup_expired_results`（每 60s 节流，static AtomicU64）

- [x] Task 12: 新功能 — 任务元数据 meta（对齐 Celery update_state meta）
  - [x] SubTask 12.1: `Task` struct 新增 `meta: Option<String>`
  - [x] SubTask 12.2: `TaskBuilder` 新增 `meta(s)` builder 方法
  - [x] SubTask 12.3: `Xhjob` 类新增 `with_meta`，PHP 暴露为 `withMeta`
  - [x] SubTask 12.4: `handle_dispatch` 自动透传（serde 自动反序列化）
  - [x] SubTask 12.5: `sqlite.rs` schema 新增 `meta TEXT` 列 + ALTER TABLE 兼容 + `task_from_row` / `insert_task` 读写
  - [x] SubTask 12.6: `StateInfo` 新增 `meta` 字段 + `xhjob_state` 返回 `meta`

- [x] Task 13: 新功能 — 任务优先级队列生效（对齐 Celery priority）
  - [x] SubTask 13.1: 审查 `process_one` 是否按 priority 排序（已实现：TaskQueue::enqueue 用 `sort_by(b.0.cmp(&a.0))` 即 priority DESC）
  - [x] SubTask 13.2: 无需修改（已实现）
  - [x] SubTask 13.3: 无需新增 trait 方法（已实现）
  - [x] SubTask 13.4: 单元测试验证 priority=10 比 priority=1 先执行（4 个测试：drain_next 顺序、equal priority FIFO、空队列 None、scan_retries 保持 priority）

- [x] Task 14: README 文档补齐（含 maxExecutions + 5 个对齐新功能）
  - [x] SubTask 14.1: Xhjob 类方法表新增 `maxExecutions` / `startAt` / `endAt` / `resultTtl` / `withMeta` 5 行
  - [x] SubTask 14.2: 顶层函数表新增 `xhjob_remove` / `xhjob_pause` / `xhjob_resume` / `xhjob_cancel` / `xhjob_list` 5 行
  - [x] SubTask 14.3: xhjob_state 返回说明新增 7 字段
  - [x] SubTask 14.4: XHJOB_PERSIST 行补充"仅在 daemon 启动时读取一次"说明
  - [x] SubTask 14.5: Cron 自定义时区节新增 5/6 段表达式格式说明
  - [x] SubTask 14.6: cron 方法说明补充"5 或 6 段表达式"
  - [x] SubTask 14.7: 新增"Cron 执行次数限制"小节
  - [x] SubTask 14.8: 新增"任务暂停 / 恢复 / 取消 / 删除"小节
  - [x] SubTask 14.9: 新增"任务起始 / 结束时间"小节
  - [x] SubTask 14.10: 新增"任务列表查询"小节
  - [x] SubTask 14.11: 新增"Misfire 处理"小节
  - [x] SubTask 14.12: 新增"任务结果过期清理"小节
  - [x] SubTask 14.13: 新增"任务元数据"小节
  - [x] SubTask 14.14: 新增"对照 APScheduler / Celery 的功能对齐"小节

- [x] Task 15: examples 完善
  - [x] SubTask 15.1: `cron_http.php` dispatch 后增加中文提示 daemon 持续触发
  - [x] SubTask 15.2: `cron_http.php` 增加 `maxExecutions(3)` 示例任务
  - [x] SubTask 15.3: 新增 `cron_lifecycle.php`（7 步演示 pause/resume/cancel/remove/list）
  - [x] SubTask 15.4: 审计其余 examples（8 个），均无 daemon 持久化误导

- [x] Task 16: 新增 `tests/boundary_cases.php` 边界场景测试
  - [x] SubTask 16.1-16.7: invalid JSON / 非法 service name / daemon 未运行 / 非法 cron / HTTP 4xx 不重试（需网络则 SKIP），PASS/FAIL 汇总

- [x] Task 17: 新增 `tests/max_executions.php` 执行次数限制测试
  - [x] SubTask 17.1-17.5: maxExecutions(3) 执行 3 次后 SUCCESS；maxExecutions(0) 无限触发验证

- [x] Task 18: 新增 `tests/lifecycle_api.php` 任务暂停/恢复/取消/删除/列表测试
  - [x] SubTask 18.1-18.7: pause/resume/cancel/remove/list + state 过滤

- [x] Task 19: 新增 `tests/start_end_date.php` 起止时间窗口测试
  - [x] SubTask 19.1-19.3: startAt 延迟触发 + endAt 后 SUCCESS

- [x] Task 20: 新增 `tests/result_ttl.php` 结果过期清理测试
  - [x] SubTask 20.1-20.5: resultTtl(2) 后 result 清理但 task 保留

- [x] Task 21: 新增 `tests/meta_field.php` 元数据字段测试
  - [x] SubTask 21.1-21.4: withMeta 写入 + xhjob_state / xhjob_list 读回

- [ ] Task 22: 重新编译 + 重跑全量测试（验证无回归）（**已废弃，由 Task 33 取代**）
  - [ ] SubTask 22.1: `cargo build --release --features persist` 退出码 0 且无 warning
  - [ ] SubTask 22.2: `cargo build --release`（默认 feature）退出码 0 且无 warning
  - [ ] SubTask 22.3: `cargo test --release --lib --features persist` 全部通过（含 retry 按错误类型测试 + next_fire 新签名测试 + 新功能单元测试）
  - [ ] SubTask 22.4: `cargo test --release --lib`（默认 feature）全部通过
  - [ ] SubTask 22.5: 重新部署 `libxhjob.so` 到 `php-config --extension-dir`
  - [ ] SubTask 22.6: `php run-tests.php tests/` 全量 .phpt 仍 7 PASS / 3 SKIP / 0 FAIL
  - [ ] SubTask 22.7: `php tests/boundary_cases.php` 全部 PASS（4xx 测试可 SKIP）
  - [ ] SubTask 22.8: `php tests/max_executions.php` 全部 PASS
  - [ ] SubTask 22.9: `php tests/lifecycle_api.php` 全部 PASS
  - [ ] SubTask 22.10: `php tests/start_end_date.php` 全部 PASS
  - [ ] SubTask 22.11: `php tests/result_ttl.php` 全部 PASS
  - [ ] SubTask 22.12: `php tests/meta_field.php` 全部 PASS
  - [ ] SubTask 22.13: `bash tests/business/cli_bus/run_all.sh` 4 步全部 PASS
  - [ ] SubTask 22.14: `php tests/business/fpm_sim/proc_test.php` 10 步全部 PASS
  - [ ] SubTask 22.15: `php tests/business/fpm_sim/client_test.php` 10 步全部 PASS
  - [ ] SubTask 22.16: `php tests/data_dir_smoke.php` PASS
  - [ ] SubTask 22.17: 8 个 `examples/*.php`（含新增 cron_lifecycle.php）全部可运行（无 fatal error）
  - [ ] SubTask 22.18: `php tests/functional_verify.php` 15 步全部 PASS（验证既有功能未回归）

- [ ] Task 23: 单独执行功能模块验证（用代码验证执行结果正确性）（**已废弃，由 Task 34 取代**）
  - [ ] SubTask 23.1: shell 任务：dispatch echo 命令，验证 stdout 与 exit_code（PASS）
  - [ ] SubTask 23.2: retry 任务：dispatch HTTP 404 + withRetry(3)，验证不重试 attempts=1（验证新 should_retry）
  - [ ] SubTask 23.3: retry 任务：dispatch shell 失败 + withRetry(3)，验证重试至多 3 次（PASS）
  - [ ] SubTask 23.4: cron 任务：tests/cron.phpt 验证 cron 触发（PASS）
  - [ ] SubTask 23.5: overlap 任务：慢任务 + allowOverlap(false)，验证排队执行（PASS）
  - [ ] SubTask 23.6: persist 任务：persist(true) + 投递，restart daemon 后验证任务状态可查（PASS）
  - [ ] SubTask 23.7: 多服务：两个服务 PID 不同且互不干扰（PASS）
  - [ ] SubTask 23.8: maxExecutions：cron + maxExecutions(3)，验证执行 3 次后停止（PASS）
  - [ ] SubTask 23.9: lifecycle：pause/resume/cancel/remove/list 全部按预期（PASS）
  - [ ] SubTask 23.10: start_end_date：startAt/endAt 时间窗口生效（PASS）
  - [ ] SubTask 23.11: result_ttl：result 过期清理（PASS）
  - [ ] SubTask 23.12: meta：meta 字段读写（PASS）
  - [ ] SubTask 23.13: priority：高优先级先执行（PASS）

- [ ] Task 24: 提交并推送远程主分支（**已废弃，由 Task 35 取代**）
  - [ ] SubTask 24.1: `git status` 核对修改文件清单
  - [ ] SubTask 24.2: `git diff` 审查改动内容（确认无意外改动、无删除用户文件）
  - [ ] SubTask 24.3: `git add <指定文件>` 暂存改动（不 `git add -A`）
  - [ ] SubTask 24.4: `git commit -m "feat: 对齐 APScheduler/Celery + maxExecutions + lifecycle API + start/end date + result_ttl + meta + priority + 文档/示例/测试补齐"` 提交到本地 main
  - [ ] SubTask 24.5: `git push origin main` 推送到远程主分支
  - [ ] SubTask 24.6: `git log origin/main --oneline -5` 确认远程 HEAD 已更新

- [x] Task 25: 修复 maxExecutions 终态判定时机 bug（cron 任务 success 后不立即 Success 终态）
  - [x] SubTask 25.1: 修改 `src/scheduler/queue.rs::process_one` 的 success 路径：对于 `task_clone.cron.is_some()` 的任务，先 `increment_execution_count` 再判定 `max_executions` 上限，未到达上限则不调用 `update_state(Success)`
  - [x] SubTask 25.2: 对于非 cron 任务（`task_clone.cron.is_none()`）保持原行为：success 后立即 `update_state(Success)`
  - [x] SubTask 25.3: 重跑 `php tests/max_executions.php` 验证 execution_count 能到达 3，6 PASS / 0 FAIL
  - [x] SubTask 25.4: 重跑 `cargo test --release --lib`（两种 feature）确保不回归
  - [x] SubTask 25.5: 新增单元测试 `test_cron_task_success_not_terminal_until_max` 验证 cron 任务未到 max_executions 时 state 仍为 Pending

- [x] Task 26: 新功能 A7 + A8 — IntervalTrigger (every) + DateTrigger (runAt)
  - [x] SubTask 26.1: `Task` struct 新增 `interval: Option<u64>` 与 `run_at: Option<i64>` 字段（带 `#[serde(default)]`）
  - [x] SubTask 26.2: `TaskBuilder` 新增 `interval` / `run_at` 字段 + builder 方法 `every(secs: u64)` / `run_at(ts: i64)` + `build()` 复制
  - [x] SubTask 26.3: `Xhjob` 类新增 `every(&mut self, secs: i64)` / `run_at(&mut self, ts: i64)`，PHP 暴露为 `every(int $secs)` / `runAt(int $ts)`
  - [x] SubTask 26.4: `scan_once` 中按优先级 `runAt > cron > interval` 调度：
    - runAt：next_fire = run_at，触发后立即置 Success 终态
    - cron：保持当前逻辑
    - interval：next_fire = now + interval，每次触发后推进
  - [x] SubTask 26.5: 同时设置时 warn：cron + every → cron 优先；runAt + every → runAt 优先；runAt + cron → runAt 优先
  - [x] SubTask 26.6: `sqlite.rs` schema 新增 `interval INTEGER` / `run_at INTEGER` 两列 + ALTER TABLE 兼容
  - [x] SubTask 26.7: `in_memory.rs` 同步字段（无需 schema）
  - [x] SubTask 26.8: `StateInfo` 新增 `interval` / `run_at` 字段 + `xhjob_state` 返回
  - [x] SubTask 26.9: 单元测试 `test_interval_trigger_next_fire_advances` 验证 every(30) next_fire 推进；`test_run_at_one_shot_terminal` 验证 runAt 触发后立即 Success
  - [x] SubTask 26.10: `tests/interval_trigger.php` 验证 every(2) 周期触发至少 2 次
  - [x] SubTask 26.11: `tests/run_at_trigger.php` 验证 runAt(time()+3) 一次性触发后 state=SUCCESS

- [x] Task 27: 新功能 A9 — Jitter（随机抖动避免惊群）
  - [x] SubTask 27.1: `Task` struct 新增 `jitter: u64` 字段（默认 0），带 `#[serde(default)]`
  - [x] SubTask 27.2: `TaskBuilder` 新增 `jitter` 字段 + builder 方法 `jitter(secs: u64)` + `build()` 复制
  - [x] SubTask 27.3: `Xhjob` 类新增 `jitter(&mut self, secs: i64)`，PHP 暴露为 `jitter(int $secs)`
  - [x] SubTask 27.4: `scan_once` 在计算 next_fire 后追加 `0..jitter` 随机偏移（用 `rand::thread_rng().gen_range(0..jitter)`）；仅对 cron / interval 任务生效；runAt 任务 warn 后忽略
  - [x] SubTask 27.5: `Cargo.toml` 新增 `rand` 依赖（若未引入）
  - [x] SubTask 27.6: `sqlite.rs` schema 新增 `jitter INTEGER NOT NULL DEFAULT 0` 列 + ALTER TABLE 兼容
  - [x] SubTask 27.7: `StateInfo` 新增 `jitter` 字段 + `xhjob_state` 返回
  - [x] SubTask 27.8: 单元测试 `test_jitter_adds_random_offset_within_range` 验证 next_fire 在 [now+1, now+jitter+next] 范围内
  - [x] SubTask 27.9: `tests/jitter_test.php` 验证 jitter(5) 多任务触发时刻分散

- [x] Task 28: 新功能 C6 — Task expires（任务级过期）
  - [x] SubTask 28.1: `TaskState` 新增 `Expired` 终态 + `as_str`/`from_str`/`is_terminal` 同步
  - [x] SubTask 28.2: `Task` struct 新增 `expires: u64` 字段（默认 0=不过期），带 `#[serde(default)]`
  - [x] SubTask 28.3: `TaskBuilder` 新增 `expires` 字段 + builder 方法 `expires(secs: u64)` + `build()` 复制
  - [x] SubTask 28.4: `Xhjob` 类新增 `expires(&mut self, secs: i64)`，PHP 暴露为 `expires(int $secs)`
  - [x] SubTask 28.5: `scan_once` 检测：`task.expires > 0 && task.created_at + expires < now && task.state == Pending` → 置 `Expired` 终态 + continue
  - [x] SubTask 28.6: 已 Running 任务不受 expires 影响（只对 Pending 生效）
  - [x] SubTask 28.7: `sqlite.rs` schema 新增 `expires INTEGER NOT NULL DEFAULT 0` 列 + ALTER TABLE 兼容
  - [x] SubTask 28.8: `StateInfo` 新增 `expires` 字段 + `xhjob_state` 返回 `expires`
  - [x] SubTask 28.9: 单元测试 `test_expires_marks_pending_task_expired` 验证超时 Pending 任务被置 Expired
  - [x] SubTask 28.10: `tests/expires_test.php` 验证 expires(2) + maxInstances 限制 2 秒后 Pending 任务被置 EXPIRED

- [x] Task 29: 新功能 C7 — Task requeue（重新入队）
  - [x] SubTask 29.1: `TaskStore` trait 新增 `requeue_task(&self, id: &str) -> Result<bool>`（返回是否成功 requeue）
  - [x] SubTask 29.2: `in_memory.rs` 实现：终态任务（Cancelled/Failed/Expired）→ state=Pending, attempts=0, next_fire=Some(now), return true；非终态 → return false
  - [x] SubTask 29.3: `sqlite.rs` 实现同样逻辑（UPDATE WHERE state IN ('CANCELLED','FAILED','EXPIRED')）
  - [x] SubTask 29.4: `daemon_main.rs` 新增 `handle_requeue_op` handler
  - [x] SubTask 29.5: `lib.rs` 新增顶层函数 `xhjob_requeue($id, $name='default', $data_dir=null): bool`
  - [x] SubTask 29.6: requeue 后立即 enqueue（让队列 `scan_retries` 重新拉取）— 通过 next_fire=now 让 scan_once 触发
  - [x] SubTask 29.7: 单元测试 `test_requeue_resets_terminal_to_pending` 验证 Cancelled/Failed/Expired 重置后为 Pending + attempts=0
  - [x] SubTask 29.8: 单元测试 `test_requeue_rejects_running_task` 验证 Running 任务 requeue 返回 false
  - [x] SubTask 29.9: `tests/requeue_test.php` 端到端验证 cancel → requeue → 重新触发流程

- [x] Task 30: 新功能 C8 — Retry exponential backoff（指数退避）
  - [x] SubTask 30.1: `Task` struct 新增 `retry_backoff: bool` 字段（默认 false），带 `#[serde(default)]`
  - [x] SubTask 30.2: `TaskBuilder` 新增 `retry_backoff` 字段 + builder 方法 `retry_backoff(on: bool)` + `build()` 复制
  - [x] SubTask 30.3: `Xhjob` 类新增 `retry_backoff(&mut self, on: bool)`，PHP 暴露为 `retryBackoff(bool $on)`
  - [x] SubTask 30.4: `src/retry/mod.rs::schedule_retry` 修改：当 `task.retry_backoff=true` 时，计算 delay 为 `min(task.retry_delay * 2^(attempts-1), task.retry_delay * 60)`
  - [x] SubTask 30.5: `retry_backoff=false` 时保持固定 retry_delay（向后兼容）
  - [x] SubTask 30.6: `sqlite.rs` schema 新增 `retry_backoff INTEGER NOT NULL DEFAULT 0` 列 + ALTER TABLE 兼容
  - [x] SubTask 30.7: `StateInfo` 新增 `retry_backoff` 字段 + `xhjob_state` 返回
  - [x] SubTask 30.8: 单元测试 `test_exponential_backoff_delay_sequence` 验证 attempts=1→1s, 2→2s, 3→4s, 4→8s, 5→16s, 上限 60s
  - [x] SubTask 30.9: `tests/retry_backoff.php` 验证 retryBackoff(true) + withRetry(3,1) + HTTP 500 URL 重试间隔递增

- [x] Task 31: README + examples 文档更新（A7-A9 + C6-C8 新对齐项）
  - [ ] SubTask 31.1: Xhjob 类方法表新增 `every` / `runAt` / `jitter` / `expires` / `retryBackoff` 5 行
  - [ ] SubTask 31.2: 顶层函数表新增 `xhjob_requeue` 行
  - [ ] SubTask 31.3: `xhjob_state` 返回说明新增 `interval` / `run_at` / `jitter` / `expires` / `retry_backoff` 5 字段
  - [ ] SubTask 31.4: TaskState 枚举说明新增 `EXPIRED`
  - [ ] SubTask 31.5: 新增"Interval 周期触发（every）"小节（A7）
  - [ ] SubTask 31.6: 新增"DateTrigger 一次性触发（runAt）"小节（A8）
  - [ ] SubTask 31.7: 新增"Jitter 随机抖动"小节（A9）
  - [ ] SubTask 31.8: 新增"任务级过期（expires）"小节（C6，对比 resultTtl 区别）
  - [ ] SubTask 31.9: 新增"任务重新入队（requeue）"小节（C7）
  - [ ] SubTask 31.10: 新增"Retry 指数退避（retryBackoff）"小节（C8）
  - [ ] SubTask 31.11: "对照 APScheduler / Celery 的功能对齐"小节扩展为 17 项（A1-A9 + C1-C8）
  - [ ] SubTask 31.12: 新增 `examples/cron_interval.php` 演示 every()
  - [ ] SubTask 31.13: 新增 `examples/cron_runAt.php` 演示 runAt()
  - [ ] SubTask 31.14: 新增 `examples/cron_jitter.php` 演示 jitter()

- [x] Task 32: 新增 6 个对齐测试文件（A7-A9 + C6-C8）
  - [ ] SubTask 32.1: `tests/interval_trigger.php` 验证 every(2) 周期触发至少 2 次（PASS）
  - [ ] SubTask 32.2: `tests/run_at_trigger.php` 验证 runAt(time()+3) 一次性触发后 state=SUCCESS（PASS）
  - [ ] SubTask 32.3: `tests/jitter_test.php` 验证 jitter(5) 多任务触发时刻分散（PASS）
  - [ ] SubTask 32.4: `tests/expires_test.php` 验证 expires(2) + maxInstances 限制后 Pending 任务被置 EXPIRED（PASS）
  - [ ] SubTask 32.5: `tests/requeue_test.php` 验证 cancel → requeue → 重新触发流程（PASS）
  - [ ] SubTask 32.6: `tests/retry_backoff.php` 验证 retryBackoff(true) + withRetry(3,1) 重试间隔递增（PASS，无网络则 SKIP）

- [ ] Task 33: 重新编译 + 重跑全量测试（含 6 个新对齐测试，**取代 Task 22**）
  - [ ] SubTask 33.1: `cargo build --release --features persist` 退出码 0 且无 warning
  - [ ] SubTask 33.2: `cargo build --release`（默认 feature）退出码 0 且无 warning
  - [ ] SubTask 33.3: `cargo test --release --lib --features persist` 全部通过（含 maxExecutions bug 修复测试 + 6 个新功能单元测试）
  - [ ] SubTask 33.4: `cargo test --release --lib`（默认 feature）全部通过
  - [ ] SubTask 33.5: 重新部署 `libxhjob.so` 到 `php-config --extension-dir`
  - [ ] SubTask 33.6: `php run-tests.php tests/` 全量 .phpt 仍 7 PASS / 3 SKIP / 0 FAIL
  - [ ] SubTask 33.7: `php tests/boundary_cases.php` 全部 PASS（4xx 测试可 SKIP）
  - [ ] SubTask 33.8: `php tests/max_executions.php` 全部 PASS（**验证 bug 修复**）
  - [ ] SubTask 33.9: `php tests/lifecycle_api.php` 全部 PASS
  - [ ] SubTask 33.10: `php tests/start_end_date.php` 全部 PASS
  - [ ] SubTask 33.11: `php tests/result_ttl.php` 全部 PASS
  - [ ] SubTask 33.12: `php tests/meta_field.php` 全部 PASS
  - [ ] SubTask 33.13: `php tests/interval_trigger.php` 全部 PASS（A7）
  - [ ] SubTask 33.14: `php tests/run_at_trigger.php` 全部 PASS（A8）
  - [ ] SubTask 33.15: `php tests/jitter_test.php` 全部 PASS（A9）
  - [ ] SubTask 33.16: `php tests/expires_test.php` 全部 PASS（C6）
  - [ ] SubTask 33.17: `php tests/requeue_test.php` 全部 PASS（C7）
  - [ ] SubTask 33.18: `php tests/retry_backoff.php` 全部 PASS（C8，无网络则 SKIP）
  - [ ] SubTask 33.19: `bash tests/business/cli_bus/run_all.sh` 4 步全部 PASS
  - [ ] SubTask 33.20: `php tests/business/fpm_sim/proc_test.php` 10 步全部 PASS
  - [ ] SubTask 33.21: `php tests/business/fpm_sim/client_test.php` 10 步全部 PASS
  - [ ] SubTask 33.22: `php tests/data_dir_smoke.php` PASS
  - [ ] SubTask 33.23: 11 个 `examples/*.php`（含新增 cron_interval / cron_runAt / cron_jitter）全部可运行（无 fatal error）
  - [ ] SubTask 33.24: `php tests/functional_verify.php` 15 步全部 PASS（验证既有功能未回归）

- [ ] Task 34: 功能模块独立验证（含 6 个新对齐功能，**取代 Task 23**）
  - [ ] SubTask 34.1: shell 任务：dispatch echo 命令，验证 stdout 与 exit_code（PASS）
  - [ ] SubTask 34.2: retry 任务：dispatch HTTP 404 + withRetry(3)，验证不重试 attempts=1
  - [ ] SubTask 34.3: retry 任务：dispatch shell 失败 + withRetry(3)，验证重试至多 3 次
  - [ ] SubTask 34.4: cron 任务：tests/cron.phpt 验证 cron 触发
  - [ ] SubTask 34.5: overlap 任务：慢任务 + allowOverlap(false)，验证排队执行
  - [ ] SubTask 34.6: persist 任务：persist(true) + 投递，restart daemon 后验证任务状态可查
  - [ ] SubTask 34.7: 多服务：两个服务 PID 不同且互不干扰
  - [ ] SubTask 34.8: maxExecutions：cron + maxExecutions(3)，验证执行 3 次后停止（**验证 bug 修复**）
  - [ ] SubTask 34.9: lifecycle：pause/resume/cancel/remove/list 全部按预期
  - [ ] SubTask 34.10: start_end_date：startAt/endAt 时间窗口生效
  - [ ] SubTask 34.11: result_ttl：result 过期清理
  - [ ] SubTask 34.12: meta：meta 字段读写
  - [ ] SubTask 34.13: priority：高优先级先执行
  - [ ] SubTask 34.14: interval (A7)：every(2) 周期触发验证
  - [ ] SubTask 34.15: runAt (A8)：一次性绝对时刻触发验证
  - [ ] SubTask 34.16: jitter (A9)：多任务触发时刻分散验证
  - [ ] SubTask 34.17: expires (C6)：超时 Pending 任务置 Expired 验证
  - [ ] SubTask 34.18: requeue (C7)：cancel → requeue → 重新触发验证
  - [ ] SubTask 34.19: retry_backoff (C8)：retryBackoff(true) 指数退避时间序列验证

- [ ] Task 35: 提交并推送远程主分支（**取代 Task 24**，含 6 个新对齐功能改动）（**已废弃，由 Task 46 取代**）
  - [ ] SubTask 35.1: `git status` 核对修改文件清单
  - [ ] SubTask 35.2: `git diff` 审查改动内容（确认无意外改动、无删除用户文件）
  - [ ] SubTask 35.3: `git add <指定文件>` 暂存改动（不 `git add -A`，避免误加 spec 文档外文件）
  - [ ] SubTask 35.4: `git commit -m "feat: 对齐 APScheduler/Celery 17 项（A1-A9+C1-C8）+ maxExecutions bug 修复 + 6 项新对齐功能（every/runAt/jitter/expires/requeue/retryBackoff）+ 文档/示例/测试补齐"` 提交到本地 main
  - [ ] SubTask 35.5: `git push origin main` 推送到远程主分支
  - [ ] SubTask 35.6: `git log origin/main --oneline -5` 确认远程 HEAD 已更新

- [x] Task 36: 新功能 A10 — max_instances(N) 真实生效（解耦 allow_overlap）
  - [x] SubTask 36.1: `src/scheduler/overlap.rs::should_fire` 升级：当 `task.max_instances > 0` 时检查 `count_running_instances(id) < task.max_instances`，否则保持当前 `allow_overlap` 逻辑
  - [x] SubTask 36.2: `allow_overlap=true` + `max_instances=1`（默认）保持当前行为（无限并发，向后兼容）；如同时设置 `max_instances=N` 则 warn 并以 N 为准
  - [x] SubTask 36.3: `allow_overlap=false` + `max_instances=N`：以 N 为准（max_instances 优先级高）
  - [x] SubTask 36.4: `TaskBuilder::max_instances(n)` builder 方法已存在，仅升级调度逻辑 + 更新 doc comment 说明"独立于 allow_overlap 生效"
  - [x] SubTask 36.5: `Xhjob` 类的 `maxInstances` PHP 方法已存在，更新 README 说明"独立于 allowOverlap 生效"
  - [x] SubTask 36.6: 单元测试 `test_max_instances_allows_n_concurrent`：N=2 时允许 2 个并发实例 + 第 3 个被跳过
  - [x] SubTask 36.7: 单元测试 `test_max_instances_default_1_no_overlap`：默认 N=1 时与 `allow_overlap=false` 等价（向后兼容）
  - [x] SubTask 36.8: 单元测试 `test_allow_overlap_true_unlimited_concurrent_backcompat`：单独 `allow_overlap=true` 仍无限并发（向后兼容）
  - [x] SubTask 36.9: `tests/max_instances_test.php` 端到端验证 maxInstances(2) 并发执行

- [x] Task 37: 新功能 A11 — reschedule_job（在线修改 cron）
  - [x] SubTask 37.1: `TaskStore` trait 新增 `reschedule_task(&self, id: &str, new_cron: &str) -> Result<bool>`（返回是否成功；非 cron 任务返回 false；终态任务返回 false）
  - [x] SubTask 37.2: `in_memory.rs` 实现：load_task → 若 `task.cron.is_none()` 返回 false；若 `state.is_terminal()` 返回 false；否则更新 cron 字段 + 重新计算 next_fire = `cron_next(now, tz)` + persist 回 store
  - [x] SubTask 37.3: `sqlite.rs` 实现同样逻辑（UPDATE tasks SET cron=?, next_fire=? WHERE id=?）
  - [x] SubTask 37.4: `daemon_main.rs` 新增 `handle_reschedule_op` handler
  - [x] SubTask 37.5: `lib.rs` 新增顶层函数 `xhjob_reschedule($id, $cron, $name='default', $data_dir=null): bool`
  - [x] SubTask 37.6: reschedule 后立即触发 scan_once 重新计算 next_fire（已通过 `reschedule_task` 写入 next_fire）
  - [x] SubTask 37.7: 单元测试 `test_reschedule_updates_cron_keeps_state`：cron='*/5 * * * *' 执行 3 次后 reschedule 为 '*/1 * * * *'，验证 cron 更新 + execution_count=3 保留 + state=Pending
  - [x] SubTask 37.8: 单元测试 `test_reschedule_rejects_non_cron_task`：interval/runAt 任务返回 false
  - [x] SubTask 37.9: 单元测试 `test_reschedule_rejects_terminal_task`：Success/Failed 任务返回 false
  - [x] SubTask 37.10: 单元测试 `test_reschedule_invalid_cron_returns_false`：非法 cron 返回 false + 错误信息含 "invalid cron"
  - [x] SubTask 37.11: `tests/reschedule_test.php` 端到端验证 reschedule 修改 cron + 保留 execution_count

- [x] Task 38: 新功能 A12 — get_job（单任务详情查询）
  - [x] SubTask 38.1: `TaskStore::load_task` 已存在返回 `Option<Task>`，复用之
  - [x] SubTask 38.2: `daemon_main.rs` 新增 `handle_get_op` handler：load_task → 若 None 返回 `{"ok":false,"error":"not found"}`；否则返回 `{"ok":true,"data": <task_json>}`
  - [x] SubTask 38.3: `lib.rs` 新增顶层函数 `xhjob_get($id, $name='default', $data_dir=null): ?string`（返回 JSON 字符串或 null）
  - [x] SubTask 38.4: 返回的 Task JSON 含所有字段（task_type / payload / cron / interval / run_at / retry_max / retry_delay / timeout / soft_timeout / priority / allow_overlap / max_instances / coalesce / max_executions / start_date / end_date / result_ttl / meta / ignore_result / acks_late / jitter / expires / retry_backoff / timezone / state / attempts / execution_count / next_fire / created_at / started_at / finished_at / last_error / paused / cancel_requested）
  - [x] SubTask 38.5: 单元测试 `test_xhjob_get_returns_full_task_json`：dispatch + xhjob_get 后 JSON 解析含 cron / retry_max / timeout 等配置字段
  - [x] SubTask 38.6: 单元测试 `test_xhjob_get_nonexistent_returns_null`：不存在 id 返回 null
  - [x] SubTask 38.7: `tests/get_job_test.php` 端到端验证 xhjob_get 返回完整字段

- [x] Task 39: 新功能 C9 — ignoreResult（fire-and-forget 不存结果）
  - [x] SubTask 39.1: `Task` struct 新增 `ignore_result: bool` 字段（默认 false），带 `#[serde(default)]`
  - [x] SubTask 39.2: `TaskBuilder` 新增 `ignore_result: bool` 字段 + builder 方法 `ignore_result(on: bool)` + `build()` 复制
  - [x] SubTask 39.3: `Xhjob` 类新增 `ignore_result(&mut self, on: bool)`，PHP 暴露为 `ignoreResult(bool $on)`
  - [x] SubTask 39.4: `process_one` 中 `dispatch_task` 成功后检查 `task.ignore_result`，若 true 跳过 `save_result` 调用
  - [x] SubTask 39.5: `ignore_result=true` + `result_ttl>0` 同时设置时 warn（ignore_result 优先）
  - [x] SubTask 39.6: `sqlite.rs` schema 新增 `ignore_result INTEGER NOT NULL DEFAULT 0` 列 + ALTER TABLE 兼容
  - [x] SubTask 39.7: `StateInfo` 新增 `ignore_result` 字段 + `xhjob_state` 返回
  - [x] SubTask 39.8: 单元测试 `test_ignore_result_skips_save_result`：ignore_result=true + dispatch + 完成后 results 表无此 task_id 行
  - [x] SubTask 39.9: 单元测试 `test_ignore_result_default_false_still_saves`：默认 false 时正常存 result
  - [x] SubTask 39.10: `tests/ignore_result_test.php` 端到端验证 ignoreResult(true) 后 xhjob_result 返回 null

- [x] Task 40: 新功能 C10 — acksLate（延迟确认 / 崩溃恢复）
  - [x] SubTask 40.1: `Task` struct 新增 `acks_late: bool` 字段（默认 false），带 `#[serde(default)]`
  - [x] SubTask 40.2: `TaskBuilder` 新增 `acks_late: bool` 字段 + builder 方法 `acks_late(on: bool)` + `build()` 复制
  - [x] SubTask 40.3: `Xhjob` 类新增 `acks_late(&mut self, on: bool)`，PHP 暴露为 `acksLate(bool $on)`
  - [x] SubTask 40.4: `TaskStore` trait 新增 `reset_running_to_pending(&self) -> Result<u64>`（扫描所有 state=Running 的任务，对 `acks_late=true` 的重置为 Pending + next_fire=now；返回重置数量）
  - [x] SubTask 40.5: `in_memory.rs` 实现：filter state=Running + acks_late=true → update state=Pending + next_fire=now
  - [x] SubTask 40.6: `sqlite.rs` 实现：`UPDATE tasks SET state='PENDING', next_fire=? WHERE state='RUNNING' AND acks_late=1`
  - [x] SubTask 40.7: `daemon_main.rs::run` 启动初始化阶段调用 `store.reset_running_to_pending()`（在 cron 调度器 + 队列启动之前）
  - [x] SubTask 40.8: `sqlite.rs` schema 新增 `acks_late INTEGER NOT NULL DEFAULT 0` 列 + ALTER TABLE 兼容
  - [x] SubTask 40.9: `StateInfo` 新增 `acks_late` 字段 + `xhjob_state` 返回
  - [x] SubTask 40.10: 单元测试 `test_reset_running_to_pending_only_acks_late`：2 个 Running 任务（acks_late=true/false），reset 后仅 acks_late=true 重置为 Pending
  - [x] SubTask 40.11: 单元测试 `test_reset_running_to_pending_skips_non_running`：Pending/Success 任务不受影响
  - [x] SubTask 40.12: `tests/acks_late_test.php` 端到端验证 dispatch + 模拟 daemon 崩溃重启后 acks_late 任务被重排

- [x] Task 41: 新功能 C11 — softTimeout（软超时优雅退出）
  - [x] SubTask 41.1: `Task` struct 新增 `soft_timeout: Option<u64>` 字段（默认 None），带 `#[serde(default)]`
  - [x] SubTask 41.2: `TaskBuilder` 新增 `soft_timeout: Option<u64>` 字段 + builder 方法 `soft_timeout(secs: u64)` + `build()` 复制 + 校验 `soft_timeout < timeout`（否则 warn 并置 None）
  - [x] SubTask 41.3: `Xhjob` 类新增 `soft_timeout(&mut self, secs: i64)`，PHP 暴露为 `softTimeout(int $secs)`
  - [x] SubTask 41.4: `src/executor/shell.rs`（或对应位置）升级超时逻辑：先在 soft_timeout 秒时发 SIGTERM（用 `nix::sys::signal` 或 `tokio::process::Child::kill` 替代为 start_kill + send_signal），等待 (timeout - soft_timeout) 秒后若未退出则 SIGKILL
  - [x] SubTask 41.5: HTTP 任务设置 soft_timeout 时 warn 并忽略（HTTP 客户端不支持优雅中断）
  - [x] SubTask 41.6: `soft_timeout=0` 等同于 None（不启用）
  - [x] SubTask 41.7: `sqlite.rs` schema 新增 `soft_timeout INTEGER` 列 + ALTER TABLE 兼容
  - [x] SubTask 41.8: `StateInfo` 新增 `soft_timeout` 字段 + `xhjob_state` 返回
  - [x] SubTask 41.9: 单元测试 `test_soft_timeout_sigterm_graceful_exit`：softTimeout(5)+timeout(10) + shell trap 处理 SIGTERM → state=Success
  - [x] SubTask 41.10: 单元测试 `test_soft_timeout_sigkill_after_grace_period`：softTimeout(5)+timeout(10) + shell 未响应 SIGTERM → 10s 后 SIGKILL → state=Failed
  - [x] SubTask 41.11: 单元测试 `test_soft_timeout_ignored_for_http`：HTTP 任务 + softTimeout(5) → warn + soft_timeout 字段为 None
  - [x] SubTask 41.12: 单元测试 `test_soft_timeout_ge_timeout_ignored`：softTimeout(15)+timeout(10) → warn + soft_timeout 字段为 None
  - [x] SubTask 41.13: `tests/soft_timeout_test.php` 端到端验证 SIGTERM 优雅退出

- [x] Task 42: README + examples 文档更新（A10-A12 + C9-C11 第三轮对齐项）
  - [ ] SubTask 42.1: Xhjob 类方法表新增 `ignoreResult(bool $on)` / `acksLate(bool $on)` / `softTimeout(int $secs)` 3 行；`maxInstances(int $n)` 行补充"独立于 allowOverlap 生效"说明
  - [ ] SubTask 42.2: 顶层函数表新增 `xhjob_reschedule` / `xhjob_get` 2 行
  - [ ] SubTask 42.3: `xhjob_state` 返回说明新增 `ignore_result` / `acks_late` / `soft_timeout` 3 字段
  - [ ] SubTask 42.4: 新增"max_instances 并发实例数（A10）"小节：说明 N=1 默认、N>1 并发、与 allowOverlap 关系
  - [ ] SubTask 42.5: 新增"reschedule 在线修改 cron（A11）"小节：说明保留 state / execution_count / meta
  - [ ] SubTask 42.6: 新增"xhjob_get 单任务详情查询（A12）"小节：说明区别于 xhjob_state（含配置字段）
  - [ ] SubTask 42.7: 新增"ignoreResult fire-and-forget（C9）"小节：说明区别于 resultTtl（不存 vs 存了再清）
  - [ ] SubTask 42.8: 新增"acksLate 崩溃恢复（C10）"小节：说明 daemon 重启后重排 Running 任务
  - [ ] SubTask 42.9: 新增"softTimeout 软超时（C11）"小节：说明 SIGTERM + 宽限期 + SIGKILL 流程
  - [ ] SubTask 42.10: "对照 APScheduler / Celery 的功能对齐"小节扩展为 23 项（A1-A12 + C1-C11）
  - [ ] SubTask 42.11: 新增 `examples/cron_maxInstances.php` 演示 maxInstances(2) 并发执行
  - [ ] SubTask 42.12: 新增 `examples/cron_reschedule.php` 演示 xhjob_reschedule 在线修改 cron

- [x] Task 43: 新增 6 个第三轮对齐测试文件（A10-A12 + C9-C11）
  - [ ] SubTask 43.1: `tests/max_instances_test.php` 验证 maxInstances(2) 允许 2 个并发 + 第 3 个被跳过（PASS）
  - [ ] SubTask 43.2: `tests/reschedule_test.php` 验证 reschedule 修改 cron + 保留 execution_count（PASS）
  - [ ] SubTask 43.3: `tests/get_job_test.php` 验证 xhjob_get 返回完整 Task JSON 字段（PASS）
  - [ ] SubTask 43.4: `tests/ignore_result_test.php` 验证 ignoreResult(true) 不存 result + state 仍正常（PASS）
  - [ ] SubTask 43.5: `tests/acks_late_test.php` 验证 daemon 重启后 acks_late 任务被重排（PASS）
  - [ ] SubTask 43.6: `tests/soft_timeout_test.php` 验证 softTimeout SIGTERM 优雅退出（PASS）

- [ ] Task 33: 重新编译 + 重跑全量测试（含 6 个新对齐测试，**取代 Task 22**）（**已废弃，由 Task 44 取代**）
  - [ ] SubTask 33.1: `cargo build --release --features persist` 退出码 0 且无 warning
  - [ ] SubTask 33.2: `cargo build --release`（默认 feature）退出码 0 且无 warning
  - [ ] SubTask 33.3: `cargo test --release --lib --features persist` 全部通过（含 maxExecutions bug 修复测试 + 6 个新功能单元测试）
  - [ ] SubTask 33.4: `cargo test --release --lib`（默认 feature）全部通过
  - [ ] SubTask 33.5: 重新部署 `libxhjob.so` 到 `php-config --extension-dir`
  - [ ] SubTask 33.6: `php run-tests.php tests/` 全量 .phpt 仍 7 PASS / 3 SKIP / 0 FAIL
  - [ ] SubTask 33.7: `php tests/boundary_cases.php` 全部 PASS（4xx 测试可 SKIP）
  - [ ] SubTask 33.8: `php tests/max_executions.php` 全部 PASS（**验证 bug 修复**）
  - [ ] SubTask 33.9: `php tests/lifecycle_api.php` 全部 PASS
  - [ ] SubTask 33.10: `php tests/start_end_date.php` 全部 PASS
  - [ ] SubTask 33.11: `php tests/result_ttl.php` 全部 PASS
  - [ ] SubTask 33.12: `php tests/meta_field.php` 全部 PASS
  - [ ] SubTask 33.13: `php tests/interval_trigger.php` 全部 PASS（A7）
  - [ ] SubTask 33.14: `php tests/run_at_trigger.php` 全部 PASS（A8）
  - [ ] SubTask 33.15: `php tests/jitter_test.php` 全部 PASS（A9）
  - [ ] SubTask 33.16: `php tests/expires_test.php` 全部 PASS（C6）
  - [ ] SubTask 33.17: `php tests/requeue_test.php` 全部 PASS（C7）
  - [ ] SubTask 33.18: `php tests/retry_backoff.php` 全部 PASS（C8，无网络则 SKIP）
  - [ ] SubTask 33.19: `bash tests/business/cli_bus/run_all.sh` 4 步全部 PASS
  - [ ] SubTask 33.20: `php tests/business/fpm_sim/proc_test.php` 10 步全部 PASS
  - [ ] SubTask 33.21: `php tests/business/fpm_sim/client_test.php` 10 步全部 PASS
  - [ ] SubTask 33.22: `php tests/data_dir_smoke.php` PASS
  - [ ] SubTask 33.23: 11 个 `examples/*.php`（含新增 cron_interval / cron_runAt / cron_jitter）全部可运行（无 fatal error）
  - [ ] SubTask 33.24: `php tests/functional_verify.php` 15 步全部 PASS（验证既有功能未回归）

- [ ] Task 34: 功能模块独立验证（含 6 个新对齐功能，**取代 Task 23**）（**已废弃，由 Task 45 取代**）
  - [ ] SubTask 34.1: shell 任务：dispatch echo 命令，验证 stdout 与 exit_code（PASS）
  - [ ] SubTask 34.2: retry 任务：dispatch HTTP 404 + withRetry(3)，验证不重试 attempts=1
  - [ ] SubTask 34.3: retry 任务：dispatch shell 失败 + withRetry(3)，验证重试至多 3 次
  - [ ] SubTask 34.4: cron 任务：tests/cron.phpt 验证 cron 触发
  - [ ] SubTask 34.5: overlap 任务：慢任务 + allowOverlap(false)，验证排队执行
  - [ ] SubTask 34.6: persist 任务：persist(true) + 投递，restart daemon 后验证任务状态可查
  - [ ] SubTask 34.7: 多服务：两个服务 PID 不同且互不干扰
  - [ ] SubTask 34.8: maxExecutions：cron + maxExecutions(3)，验证执行 3 次后停止（**验证 bug 修复**）
  - [ ] SubTask 34.9: lifecycle：pause/resume/cancel/remove/list 全部按预期
  - [ ] SubTask 34.10: start_end_date：startAt/endAt 时间窗口生效
  - [ ] SubTask 34.11: result_ttl：result 过期清理
  - [ ] SubTask 34.12: meta：meta 字段读写
  - [ ] SubTask 34.13: priority：高优先级先执行
  - [ ] SubTask 34.14: interval (A7)：every(2) 周期触发验证
  - [ ] SubTask 34.15: runAt (A8)：一次性绝对时刻触发验证
  - [ ] SubTask 34.16: jitter (A9)：多任务触发时刻分散验证
  - [ ] SubTask 34.17: expires (C6)：超时 Pending 任务置 Expired 验证
  - [ ] SubTask 34.18: requeue (C7)：cancel → requeue → 重新触发验证
  - [ ] SubTask 34.19: retry_backoff (C8)：retryBackoff(true) 指数退避时间序列验证

- [ ] Task 44: 重新编译 + 重跑全量测试（含 12 个新对齐测试：6 项 round 2 + 6 项 round 3，**取代 Task 33**）（**已废弃，由 Task 55 取代**）
  - [ ] SubTask 44.1: `cargo build --release --features persist` 退出码 0 且无 warning
  - [ ] SubTask 44.2: `cargo build --release`（默认 feature）退出码 0 且无 warning
  - [ ] SubTask 44.3: `cargo test --release --lib --features persist` 全部通过（含 maxExecutions bug 修复 + 12 个新功能单元测试）
  - [ ] SubTask 44.4: `cargo test --release --lib`（默认 feature）全部通过
  - [ ] SubTask 44.5: 重新部署 `libxhjob.so` 到 `php-config --extension-dir`
  - [ ] SubTask 44.6: `php run-tests.php tests/` 全量 .phpt 仍 7 PASS / 3 SKIP / 0 FAIL
  - [ ] SubTask 44.7: `php tests/boundary_cases.php` 全部 PASS（4xx 测试可 SKIP）
  - [ ] SubTask 44.8: `php tests/max_executions.php` 全部 PASS（**验证 bug 修复**）
  - [ ] SubTask 44.9: `php tests/lifecycle_api.php` 全部 PASS
  - [ ] SubTask 44.10: `php tests/start_end_date.php` 全部 PASS
  - [ ] SubTask 44.11: `php tests/result_ttl.php` 全部 PASS
  - [ ] SubTask 44.12: `php tests/meta_field.php` 全部 PASS
  - [ ] SubTask 44.13: `php tests/interval_trigger.php` 全部 PASS（A7）
  - [ ] SubTask 44.14: `php tests/run_at_trigger.php` 全部 PASS（A8）
  - [ ] SubTask 44.15: `php tests/jitter_test.php` 全部 PASS（A9）
  - [ ] SubTask 44.16: `php tests/expires_test.php` 全部 PASS（C6）
  - [ ] SubTask 44.17: `php tests/requeue_test.php` 全部 PASS（C7）
  - [ ] SubTask 44.18: `php tests/retry_backoff.php` 全部 PASS（C8，无网络则 SKIP）
  - [ ] SubTask 44.19: `php tests/max_instances_test.php` 全部 PASS（A10）
  - [ ] SubTask 44.20: `php tests/reschedule_test.php` 全部 PASS（A11）
  - [ ] SubTask 44.21: `php tests/get_job_test.php` 全部 PASS（A12）
  - [ ] SubTask 44.22: `php tests/ignore_result_test.php` 全部 PASS（C9）
  - [ ] SubTask 44.23: `php tests/acks_late_test.php` 全部 PASS（C10）
  - [ ] SubTask 44.24: `php tests/soft_timeout_test.php` 全部 PASS（C11）
  - [ ] SubTask 44.25: `bash tests/business/cli_bus/run_all.sh` 4 步全部 PASS
  - [ ] SubTask 44.26: `php tests/business/fpm_sim/proc_test.php` 10 步全部 PASS
  - [ ] SubTask 44.27: `php tests/business/fpm_sim/client_test.php` 10 步全部 PASS
  - [ ] SubTask 44.28: `php tests/data_dir_smoke.php` PASS
  - [ ] SubTask 44.29: 13 个 `examples/*.php`（含新增 cron_maxInstances / cron_reschedule）全部可运行（无 fatal error）
  - [ ] SubTask 44.30: `php tests/functional_verify.php` 15 步全部 PASS（验证既有功能未回归）

- [ ] Task 45: 功能模块独立验证（含 12 个新对齐功能：6 项 round 2 + 6 项 round 3，**取代 Task 34**）（**已废弃，由 Task 56 取代**）
  - [ ] SubTask 45.1: shell 任务：dispatch echo 命令，验证 stdout 与 exit_code（PASS）
  - [ ] SubTask 45.2: retry 任务：dispatch HTTP 404 + withRetry(3)，验证不重试 attempts=1
  - [ ] SubTask 45.3: retry 任务：dispatch shell 失败 + withRetry(3)，验证重试至多 3 次
  - [ ] SubTask 45.4: cron 任务：tests/cron.phpt 验证 cron 触发
  - [ ] SubTask 45.5: overlap 任务：慢任务 + allowOverlap(false)，验证排队执行
  - [ ] SubTask 45.6: persist 任务：persist(true) + 投递，restart daemon 后验证任务状态可查
  - [ ] SubTask 45.7: 多服务：两个服务 PID 不同且互不干扰
  - [ ] SubTask 45.8: maxExecutions：cron + maxExecutions(3)，验证执行 3 次后停止（**验证 bug 修复**）
  - [ ] SubTask 45.9: lifecycle：pause/resume/cancel/remove/list 全部按预期
  - [ ] SubTask 45.10: start_end_date：startAt/endAt 时间窗口生效
  - [ ] SubTask 45.11: result_ttl：result 过期清理
  - [ ] SubTask 45.12: meta：meta 字段读写
  - [ ] SubTask 45.13: priority：高优先级先执行
  - [ ] SubTask 45.14: interval (A7)：every(2) 周期触发验证
  - [ ] SubTask 45.15: runAt (A8)：一次性绝对时刻触发验证
  - [ ] SubTask 45.16: jitter (A9)：多任务触发时刻分散验证
  - [ ] SubTask 45.17: expires (C6)：超时 Pending 任务置 Expired 验证
  - [ ] SubTask 45.18: requeue (C7)：cancel → requeue → 重新触发验证
  - [ ] SubTask 45.19: retry_backoff (C8)：retryBackoff(true) 指数退避时间序列验证
  - [ ] SubTask 45.20: max_instances (A10)：maxInstances(2) 允许 2 个并发实例 + 第 3 个被跳过验证
  - [ ] SubTask 45.21: reschedule (A11)：reschedule 修改 cron + 保留 execution_count 验证
  - [ ] SubTask 45.22: get_job (A12)：xhjob_get 返回完整 Task JSON 字段验证
  - [ ] SubTask 45.23: ignore_result (C9)：ignoreResult(true) 不存 result + state 仍正常验证
  - [ ] SubTask 45.24: acks_late (C10)：daemon 重启后 acks_late 任务被重排验证
  - [ ] SubTask 45.25: soft_timeout (C11)：softTimeout(5)+timeout(10) shell 任务 SIGTERM 优雅退出验证

- [ ] Task 46: 提交并推送远程主分支（**取代 Task 35**，含 12 个新对齐功能改动：6 项 round 2 + 6 项 round 3）（**已废弃，由 Task 57 取代**）
  - [ ] SubTask 46.1: `git status` 核对修改文件清单
  - [ ] SubTask 46.2: `git diff` 审查改动内容（确认无意外改动、无删除用户文件）
  - [ ] SubTask 46.3: `git add <指定文件>` 暂存改动（不 `git add -A`，避免误加 spec 文档外文件）
  - [ ] SubTask 46.4: `git commit -m "feat: 对齐 APScheduler/Celery 23 项（A1-A12+C1-C11）+ maxExecutions bug 修复 + 12 项新对齐功能（every/runAt/jitter/expires/requeue/retryBackoff/maxInstances升级/reschedule/get/ignoreResult/acksLate/softTimeout）+ 文档/示例/测试补齐"` 提交到本地 main
  - [ ] SubTask 46.5: `git push origin main` 推送到远程主分支
  - [ ] SubTask 46.6: `git log origin/main --oneline -5` 确认远程 HEAD 已更新

- [x] Task 47: 新功能 A13 — misfire_grace_time 每作业级（per-job 覆盖全局默认 60s）
  - [ ] SubTask 47.1: `Task` struct 新增 `misfire_grace_time: u64` 字段（默认 0=使用全局默认 60s），带 `#[serde(default)]`
  - [ ] SubTask 47.2: `TaskBuilder` 新增 `misfire_grace_time: u64` 字段 + builder 方法 `misfire_grace_time(secs: u64)` + `build()` 复制
  - [ ] SubTask 47.3: `Xhjob` 类新增 `misfire_grace_time(&mut self, secs: i64)`，PHP 暴露为 `misfireGraceTime(int $secs): $this`
  - [ ] SubTask 47.4: `scan_once` 中检测 `now > next_fire` 时按 `grace_time = if task.misfire_grace_time > 0 { task.misfire_grace_time } else { 60 }` 判定：若 `now - next_fire > grace_time` 视为 misfire，按 `coalesce=true` 跳过本次触发并推进 next_fire / `coalesce=false` 丢弃并推进；否则仍触发
  - [ ] SubTask 47.5: 非 cron 任务（interval / runAt）设置 `misfireGraceTime` 时 warn 并忽略（interval/runAt 无 misfire 概念）
  - [ ] SubTask 47.6: `sqlite.rs` schema 新增 `misfire_grace_time INTEGER NOT NULL DEFAULT 0` 列 + ALTER TABLE 兼容
  - [ ] SubTask 47.7: `in_memory.rs` 同步字段（无需 schema）
  - [ ] SubTask 47.8: `StateInfo` 新增 `misfire_grace_time` 字段 + `xhjob_state` 返回
  - [ ] SubTask 47.9: 单元测试 `test_misfire_grace_time_per_job_short_window_skips`：misfireGraceTime(5) + 模拟 6s 延迟 → 跳过触发并推进 next_fire
  - [ ] SubTask 47.10: 单元测试 `test_misfire_grace_time_per_job_long_window_fires`：misfireGraceTime(300) + 60s 延迟 → 仍触发 execution_count 递增
  - [ ] SubTask 47.11: 单元测试 `test_misfire_grace_time_default_0_uses_global_60s`：默认 0 时使用全局 60s 判定（向后兼容）
  - [ ] SubTask 47.12: 单元测试 `test_misfire_grace_time_ignored_for_non_cron`：interval/runAt 任务 warn 并忽略
  - [ ] SubTask 47.13: `tests/misfire_grace_time_test.php` 端到端验证 misfireGraceTime(5) 与全局默认对比 + coalesce 配合

- [x] Task 48: 新功能 A14 — replace_existing 幂等 dispatch（withId + replaceExisting）
  - [ ] SubTask 48.1: `Task` struct 新增 `id: Option<String>` 字段（自定义 id，None 时系统生成 UUID），带 `#[serde(default)]`
  - [ ] SubTask 48.2: `Task` struct 新增 `replace_existing: bool` 字段（默认 false），带 `#[serde(default)]`
  - [ ] SubTask 48.3: `TaskBuilder` 新增 `id: Option<String>` + `replace_existing: bool` 字段 + builder 方法 `id(s: impl Into<String>)` / `replace_existing(on: bool)` + `build()` 复制
  - [ ] SubTask 48.4: `Xhjob` 类新增 `id(&mut self, s: &str)`（PHP 暴露为 `withId(string $id): $this`）+ `replace_existing(&mut self, on: bool)`（PHP 暴露为 `replaceExisting(bool $on): $this`）
  - [ ] SubTask 48.5: `handle_dispatch` / `dispatch` 中处理 id 冲突：若 `task.id.is_some() && store.load_task(id).is_some()`：
    - `replace_existing=true`：先 `store.remove_task(id)` 再 `store.insert_task(task)`（完全替换，state/attempts/execution_count 重置）
    - `replace_existing=false`（默认）：返回 `error: task id already exists: <id>`
  - [ ] SubTask 48.6: `task.id.is_none()` 时系统生成 UUID（保持当前行为，replace_existing 无效果）
  - [ ] SubTask 48.7: `sqlite.rs` schema 无需新增列（id 已是主键，replace_existing 仅控制 dispatch 行为）；但 `insert_task` 需在主键冲突时返回明确错误（INSERT OR FAIL）
  - [ ] SubTask 48.8: `StateInfo` / `xhjob_get` 返回 id 字段（已存在）
  - [ ] SubTask 48.9: 单元测试 `test_replace_existing_replaces_same_id`：withId('x')->replaceExisting(true) 两次 dispatch → 第二次覆盖第一次
  - [ ] SubTask 48.10: 单元测试 `test_replace_existing_false_errors_on_conflict`：withId('x') 两次 dispatch（默认 false）→ 第二次返回 error
  - [ ] SubTask 48.11: 单元测试 `test_replace_existing_no_effect_without_id`：不调用 withId + replaceExisting(true) → 系统生成 UUID 必然不冲突，行为与当前一致
  - [ ] SubTask 48.12: 单元测试 `test_replace_existing_overrides_terminal_task`：withId('x')->maxExecutions(3) 执行 3 次终态后 replaceExisting(true) 覆盖 → 新 task 状态 Pending
  - [ ] SubTask 48.13: `tests/replace_existing_test.php` 端到端验证部署脚本幂等场景

- [x] Task 49: 新功能 A15 — tags 作业分组（tags 标记 + xhjob_list 按 tag 过滤）
  - [ ] SubTask 49.1: `Task` struct 新增 `tags: Vec<String>` 字段（默认空 Vec），带 `#[serde(default)]`
  - [ ] SubTask 49.2: `TaskBuilder` 新增 `tags: Vec<String>` 字段 + builder 方法 `tags(tags: &[&str])`（接受切片）+ `build()` 复制
  - [ ] SubTask 49.3: `Xhjob` 类新增 `tags(&mut self, tags: Vec<String>)`，PHP 暴露为 `tags(array $tags): $this`（PHP 端接收数组转 Vec<String>）
  - [ ] SubTask 49.4: `TaskSummary` struct 已存在，新增 `tags: Vec<String>` 字段（从 Task 复制）
  - [ ] SubTask 49.5: `TaskStore::list_tasks` trait 方法签名扩展为 `list_tasks(&self, state_filter: Option<TaskState>, tag_filter: Option<&str>) -> Result<Vec<TaskSummary>>`（tag_filter 为 Some 时过滤 tags 数组包含该值）
  - [ ] SubTask 49.6: `in_memory.rs` 实现 tag 过滤：`tasks.iter().filter(|t| tag_filter.map_or(true, |tag| t.tags.contains(tag))`
  - [ ] SubTask 49.7: `sqlite.rs` 实现 tag 过滤：`WHERE tags LIKE '%"tag"%'`（JSON LIKE 模式，注意转义双引号）；或读出后 Rust 端 filter（简单但效率低，建议用 LIKE）
  - [ ] SubTask 49.8: `sqlite.rs` schema 新增 `tags TEXT NOT NULL DEFAULT '[]'` 列（存 JSON 数组字符串，旧库 ALTER TABLE 兼容）；`task_from_row` 反序列化 JSON / `insert_task` 序列化 JSON
  - [ ] SubTask 49.9: IPC 命令 `list` 协议扩展：参数 `state_filter: Option<String>` + `tag: Option<String>`
  - [ ] SubTask 49.10: `daemon_main.rs::handle_list_op` 接收新 tag 参数透传 store
  - [ ] SubTask 49.11: `lib.rs::xhjob_list` 函数签名扩展为 `xhjob_list($name='default', $state_filter=null, $tag=null, $data_dir=null): string`（新增可选 $tag 参数）
  - [ ] SubTask 49.12: `StateInfo` 新增 `tags` 字段 + `xhjob_state` 返回 tags 数组
  - [ ] SubTask 49.13: 单元测试 `test_tags_filter_by_single_tag`：3 个任务含不同 tags，list_tasks(tag=Some("reports")) 返回含 reports 的 2 个
  - [ ] SubTask 49.14: 单元测试 `test_tags_default_empty_no_filter`：不调用 tags 时 tags=[]，list_tasks(tag=Some("reports")) 不返回该任务
  - [ ] SubTask 49.15: 单元测试 `test_tags_multi_tag_match_any`：tags=['reports','critical'] 的任务，list_tasks(tag=Some("critical")) 也返回
  - [ ] SubTask 49.16: 单元测试 `test_tags_persisted_after_restart`：tags 持久化到 SQLite，restart 后 xhjob_get 仍返回 tags
  - [ ] SubTask 49.17: `tests/tags_test.php` 端到端验证 tags 标记 + xhjob_list 按 tag 过滤

- [x] Task 50: 新功能 C12 — rate_limit 每任务限流（滑动窗口限制单位时间触发数）
  - [ ] SubTask 50.1: `Task` struct 新增 `rate_limit_count: u32`（默认 0=不限流）+ `rate_limit_window: u64`（默认 0）字段，带 `#[serde(default)]`
  - [ ] SubTask 50.2: `TaskBuilder` 新增 `rate_limit_count: u32` + `rate_limit_window: u64` 字段 + builder 方法 `rate_limit(max_count: u32, window_secs: u64)` + `build()` 复制
  - [ ] SubTask 50.3: `Xhjob` 类新增 `rate_limit(&mut self, max_count: i64, window_secs: i64)`，PHP 暴露为 `rateLimit(int $maxCount, int $windowSecs): $this`
  - [ ] SubTask 50.4: 新增 `src/scheduler/rate_limit.rs` 模块：`RateLimiter` struct 持有 `HashMap<task_id, VecDeque<i64>>`（task_id → 最近触发时间戳队列）；方法 `check_and_record(&mut self, task_id: &str, now: i64, max_count: u32, window_secs: u64) -> bool`（true=允许触发，false=限流）；内部清理过期时间戳
  - [ ] SubTask 50.5: `OverlapController` 或 `scan_once` 触发前调用 `RateLimiter::check_and_record`：返回 false 时跳过本次触发并推进 next_fire 至下一轮 cron 时刻
  - [ ] SubTask 50.6: daemon 重启时从 store 中 `started_at > now - window_secs` 的任务记录重建 sliding window 计数（避免重启后限流失效）
  - [ ] SubTask 50.7: `rate_limit_count=0`（默认）时不限流，行为与当前一致（向后兼容）
  - [ ] SubTask 50.8: `sqlite.rs` schema 新增 `rate_limit_count INTEGER NOT NULL DEFAULT 0` + `rate_limit_window INTEGER NOT NULL DEFAULT 0` 两列 + ALTER TABLE 兼容
  - [ ] SubTask 50.9: `StateInfo` 新增 `rate_limit_count` / `rate_limit_window` 字段 + `xhjob_state` 返回
  - [ ] SubTask 50.10: 单元测试 `test_rate_limit_allows_n_in_window`：rateLimit(3, 10) + 模拟 5 次 tick → 前 3 次允许 / 第 4-5 次限流
  - [ ] SubTask 50.11: 单元测试 `test_rate_limit_window_advances`：rateLimit(3, 10) + 第 11 秒时窗口滑过 → 第 4 次允许
  - [ ] SubTask 50.12: 单元测试 `test_rate_limit_default_0_no_limit`：默认 0 时不受限流（向后兼容）
  - [ ] SubTask 50.13: 单元测试 `test_rate_limit_distinct_from_max_instances`：maxInstances(1) + rateLimit(3, 60) 同时设置 → maxInstances 限制并发 1 / rateLimit 限制 60s 内 3 次，独立生效
  - [ ] SubTask 50.14: 单元测试 `test_rate_limit_rebuild_after_restart`：daemon 重启后从 store started_at 重建窗口计数
  - [ ] SubTask 50.15: `tests/rate_limit_test.php` 端到端验证 rateLimit(3, 10) 在 10s 窗口内最多 3 次触发

- [x] Task 51: 新功能 C13 — acks_on_failure 失败不放弃（与 acksLate 互补）
  - [ ] SubTask 51.1: `Task` struct 新增 `acks_on_failure: bool` 字段（默认 true 保持当前行为），带 `#[serde(default)]`
  - [ ] SubTask 51.2: `TaskBuilder` 新增 `acks_on_failure: bool` 字段 + builder 方法 `acks_on_failure(on: bool)` + `build()` 复制
  - [ ] SubTask 51.3: `Xhjob` 类新增 `acks_on_failure(&mut self, on: bool)`，PHP 暴露为 `acksOnFailure(bool $on): $this`
  - [ ] SubTask 51.4: `src/scheduler/queue.rs::process_one` 中任务执行失败路径：检查 `task.acks_on_failure`：
    - `true`（默认）：保持当前行为（按 retry_max 重试或终态 Failed）
    - `false`：重置 state=Pending + next_fire=now+retry_delay（指数退避若启用），**忽略 retry_max 上限**，任务持续重试直到成功或被 cancel/remove
  - [ ] SubTask 51.5: `acks_on_failure=false` 时 retry_max 字段被忽略（attempts 仍递增用于退避计算，但不作为终态判定条件）
  - [ ] SubTask 51.6: `acks_on_failure=false` 任务被 `xhjob_cancel` 时尊重 cancel 信号（cancel 优先于 acks_on_failure，state=Cancelled 终态）
  - [ ] SubTask 51.7: `acks_on_failure=false` 任务被 `xhjob_remove` 时直接删除（不受 acks_on_failure 影响）
  - [ ] SubTask 51.8: `acks_on_failure=true`（默认）保持当前行为，向后兼容
  - [ ] SubTask 51.9: `sqlite.rs` schema 新增 `acks_on_failure INTEGER NOT NULL DEFAULT 1` 列（默认 1=true）+ ALTER TABLE 兼容
  - [ ] SubTask 51.10: `StateInfo` 新增 `acks_on_failure` 字段 + `xhjob_state` 返回
  - [ ] SubTask 51.11: 单元测试 `test_acks_on_failure_false_continues_retry_past_max`：withRetry(3,1)+acksOnFailure(false)+viaShell('exit 7') → 第 4 次失败仍 state=Pending 不终态
  - [ ] SubTask 51.12: 单元测试 `test_acks_on_failure_default_true_respects_max`：默认 true 时 retry_max=3 → 第 4 次失败 state=Failed（向后兼容）
  - [ ] SubTask 51.13: 单元测试 `test_acks_on_failure_false_with_backoff`：acksOnFailure(false)+retryBackoff(true) → 指数退避持续重试不受 retry_max 限制
  - [ ] SubTask 51.14: 单元测试 `test_acks_on_failure_false_respects_cancel`：acksOnFailure(false) 持续重试中调用 xhjob_cancel → state=Cancelled 终态停止重试
  - [ ] SubTask 51.15: 单元测试 `test_acks_on_false_plus_acks_late_complementary`：acksOnFailure(false)+acksLate(true) → daemon 崩溃重启后 acksLate 重排 + acksOnFailure 让失败持续重试
  - [ ] SubTask 51.16: `tests/acks_on_failure_test.php` 端到端验证失败任务持续重试

- [x] Task 52: 新功能 C14 — worker_max_tasks_per_child daemon 自我回收（环境变量配置 + 优雅退出）
  - [ ] SubTask 52.1: `daemon_main.rs::Config` 新增 `max_tasks_per_child: u64` 字段（从环境变量 `XHJOB_MAX_TASKS_PER_CHILD` 读取，默认 0=不回收）
  - [ ] SubTask 52.2: `daemon_main.rs::run` 中维护 `tasks_executed_since_start: AtomicU64`（全局原子计数器，避免锁竞争）
  - [ ] SubTask 52.3: `process_one` 完成后（无论成功失败）`tasks_executed_since_start.fetch_add(1, Ordering::Relaxed)` 递增
  - [ ] SubTask 52.4: 检查 `if max_tasks_per_child > 0 && tasks_executed_since_start.load() >= max_tasks_per_child`：触发优雅退出流程
  - [ ] SubTask 52.5: 优雅退出流程：通知 scheduler 停止接受新触发 / 通知 queue 等待 in-flight 任务完成 / flush store / log INFO "max_tasks_per_child reached, exiting" / exit 0
  - [ ] SubTask 52.6: `max_tasks_per_child=0`（默认）时不检查不退出，行为与当前一致（向后兼容）
  - [ ] SubTask 52.7: 优雅退出等待 in-flight 任务完成（不强制 kill Running 任务），最长等待 timeout 秒后强制退出
  - [ ] SubTask 52.8: daemon 重启后计数器从 0 开始（计数器不持久化，仅内存）
  - [ ] SubTask 52.9: 单元测试 `test_max_tasks_per_child_triggers_exit_after_n`：max_tasks_per_child=5 + dispatch 5 个任务 → 第 5 个完成后 daemon 退出（用测试模式模拟，不真启动 daemon 进程）
  - [ ] SubTask 52.10: 单元测试 `test_max_tasks_per_child_default_0_no_exit`：默认 0 时 dispatch N 个任务后 daemon 不退出（向后兼容）
  - [ ] SubTask 52.11: 单元测试 `test_max_tasks_per_child_waits_inflight`：max=5 + 第 5 个任务 Running 中 → daemon 等待任务完成后才退出
  - [ ] SubTask 52.12: 单元测试 `test_max_tasks_per_child_counter_resets_on_restart`：daemon 重启后计数器从 0 开始
  - [ ] SubTask 52.13: `tests/max_tasks_per_child_test.php` 端到端验证 `XHJOB_MAX_TASKS_PER_CHILD=5 xhjob_daemon` 执行 5 个任务后退出

- [x] Task 53: README + examples 文档更新（A13-A15 + C12-C14 第四轮对齐项）
  - [ ] SubTask 53.1: Xhjob 类方法表新增 `misfireGraceTime(int $secs): $this` / `withId(string $id): $this` / `replaceExisting(bool $on): $this` / `tags(array $tags): $this` / `rateLimit(int $maxCount, int $windowSecs): $this` / `acksOnFailure(bool $on): $this` 6 行
  - [ ] SubTask 53.2: 顶层函数 `xhjob_list` 行更新签名为 `xhjob_list($name='default', $state_filter=null, $tag=null, $data_dir=null): string`（含可选 $tag）
  - [ ] SubTask 53.3: `xhjob_state` 返回说明新增 `misfire_grace_time` / `tags` / `rate_limit_count` / `rate_limit_window` / `acks_on_failure` 字段
  - [ ] SubTask 53.4: 环境变量表新增 `XHJOB_MAX_TASKS_PER_CHILD` 行说明"daemon 执行 N 个任务后自我回收，由外部进程管理器重启；默认 0=不回收"
  - [ ] SubTask 53.5: 新增"misfire_grace_time 每作业级（A13）"小节：说明 per-job 字段 vs 全局默认 60s、与 coalesce 配合规则、适用场景
  - [ ] SubTask 53.6: 新增"replace_existing 幂等 dispatch（A14）"小节：说明 withId / replaceExisting 用法 + 部署脚本幂等场景
  - [ ] SubTask 53.7: 新增"tags 作业分组（A15）"小节：说明 tags 用法 + xhjob_list 按 tag 过滤 + 适用场景
  - [ ] SubTask 53.8: 新增"rateLimit 每任务限流（C12）"小节：说明滑动窗口算法 + 与 maxInstances 区别 + 适用场景
  - [ ] SubTask 53.9: 新增"acksOnFailure 失败不放弃（C13）"小节：说明与 acksLate 互补 + 关键任务永不放弃场景
  - [ ] SubTask 53.10: 新增"worker_max_tasks_per_child daemon 自我回收（C14）"小节：说明环境变量配置 + 与 systemd / supervisor / docker restart=always 配合
  - [ ] SubTask 53.11: "对照 APScheduler / Celery 的功能对齐"小节扩展为 29 项（A1-A15 + C1-C14）
  - [ ] SubTask 53.12: 新增 `examples/cron_replace_existing.php`：演示 withId + replaceExisting 部署脚本幂等注册（A14）
  - [ ] SubTask 53.13: 新增 `examples/cron_tags.php`：演示 tags 标记 + xhjob_list 按 tag 过滤（A15）
  - [ ] SubTask 53.14: 新增 `examples/cron_rateLimit.php`：演示 rateLimit 限流外部 API 调用（C12）

- [x] Task 54: 新增 6 个第四轮对齐测试文件（A13-A15 + C12-C14）
  - [ ] SubTask 54.1: `tests/misfire_grace_time_test.php` 验证 misfireGraceTime(5) 短窗口跳过 6s 延迟 + 默认 0 用全局 60s（PASS）
  - [ ] SubTask 54.2: `tests/replace_existing_test.php` 验证 withId + replaceExisting(true) 同 id 重跑覆盖 + replaceExisting(false) 报错（PASS）
  - [ ] SubTask 54.3: `tests/tags_test.php` 验证 tags 标记 + xhjob_list 按 tag 过滤 + 默认空数组不返回（PASS）
  - [ ] SubTask 54.4: `tests/rate_limit_test.php` 验证 rateLimit(3, 10) 在 10s 窗口内最多 3 次触发 + 第 11 秒窗口滑过允许第 4 次（PASS）
  - [ ] SubTask 54.5: `tests/acks_on_failure_test.php` 验证 acksOnFailure(false) 失败任务持续重试忽略 retry_max + 被 cancel 终止（PASS）
  - [ ] SubTask 54.6: `tests/max_tasks_per_child_test.php` 验证 XHJOB_MAX_TASKS_PER_CHILD=5 daemon 执行 5 个任务后优雅退出（PASS）

- [ ] Task 55: 重新编译 + 重跑全量测试（含 18 个新对齐测试：6 项 round 2 + 6 项 round 3 + 6 项 round 4，**取代 Task 44**）（**已废弃，由 Task 66 取代**）
  - [ ] SubTask 55.1: `cargo build --release --features persist` 退出码 0 且无 warning
  - [ ] SubTask 55.2: `cargo build --release`（默认 feature）退出码 0 且无 warning
  - [ ] SubTask 55.3: `cargo test --release --lib --features persist` 全部通过（含 maxExecutions bug 修复 + 18 个新功能单元测试）
  - [ ] SubTask 55.4: `cargo test --release --lib`（默认 feature）全部通过
  - [ ] SubTask 55.5: 重新部署 `libxhjob.so` 到 `php-config --extension-dir`
  - [ ] SubTask 55.6: `php run-tests.php tests/` 全量 .phpt 仍 7 PASS / 3 SKIP / 0 FAIL
  - [ ] SubTask 55.7: `php tests/boundary_cases.php` 全部 PASS（4xx 测试可 SKIP）
  - [ ] SubTask 55.8: `php tests/max_executions.php` 全部 PASS（**验证 bug 修复**）
  - [ ] SubTask 55.9: `php tests/lifecycle_api.php` 全部 PASS
  - [ ] SubTask 55.10: `php tests/start_end_date.php` 全部 PASS
  - [ ] SubTask 55.11: `php tests/result_ttl.php` 全部 PASS
  - [ ] SubTask 55.12: `php tests/meta_field.php` 全部 PASS
  - [ ] SubTask 55.13: `php tests/interval_trigger.php` 全部 PASS（A7）
  - [ ] SubTask 55.14: `php tests/run_at_trigger.php` 全部 PASS（A8）
  - [ ] SubTask 55.15: `php tests/jitter_test.php` 全部 PASS（A9）
  - [ ] SubTask 55.16: `php tests/expires_test.php` 全部 PASS（C6）
  - [ ] SubTask 55.17: `php tests/requeue_test.php` 全部 PASS（C7）
  - [ ] SubTask 55.18: `php tests/retry_backoff.php` 全部 PASS（C8，无网络则 SKIP）
  - [ ] SubTask 55.19: `php tests/max_instances_test.php` 全部 PASS（A10）
  - [ ] SubTask 55.20: `php tests/reschedule_test.php` 全部 PASS（A11）
  - [ ] SubTask 55.21: `php tests/get_job_test.php` 全部 PASS（A12）
  - [ ] SubTask 55.22: `php tests/ignore_result_test.php` 全部 PASS（C9）
  - [ ] SubTask 55.23: `php tests/acks_late_test.php` 全部 PASS（C10）
  - [ ] SubTask 55.24: `php tests/soft_timeout_test.php` 全部 PASS（C11）
  - [ ] SubTask 55.25: `php tests/misfire_grace_time_test.php` 全部 PASS（A13）
  - [ ] SubTask 55.26: `php tests/replace_existing_test.php` 全部 PASS（A14）
  - [ ] SubTask 55.27: `php tests/tags_test.php` 全部 PASS（A15）
  - [ ] SubTask 55.28: `php tests/rate_limit_test.php` 全部 PASS（C12）
  - [ ] SubTask 55.29: `php tests/acks_on_failure_test.php` 全部 PASS（C13）
  - [ ] SubTask 55.30: `php tests/max_tasks_per_child_test.php` 全部 PASS（C14）
  - [ ] SubTask 55.31: `bash tests/business/cli_bus/run_all.sh` 4 步全部 PASS
  - [ ] SubTask 55.32: `php tests/business/fpm_sim/proc_test.php` 10 步全部 PASS
  - [ ] SubTask 55.33: `php tests/business/fpm_sim/client_test.php` 10 步全部 PASS
  - [ ] SubTask 55.34: `php tests/data_dir_smoke.php` PASS
  - [ ] SubTask 55.35: 16 个 `examples/*.php`（含新增 cron_replace_existing / cron_tags / cron_rateLimit）全部可运行（无 fatal error）
  - [ ] SubTask 55.36: `php tests/functional_verify.php` 15 步全部 PASS（验证既有功能未回归）

- [ ] Task 56: 功能模块独立验证（含 18 个新对齐功能：6 项 round 2 + 6 项 round 3 + 6 项 round 4，**取代 Task 45**）（**已废弃，由 Task 67 取代**）
  - [ ] SubTask 56.1: shell 任务：dispatch echo 命令，验证 stdout 与 exit_code（PASS）
  - [ ] SubTask 56.2: retry 任务：dispatch HTTP 404 + withRetry(3)，验证不重试 attempts=1
  - [ ] SubTask 56.3: retry 任务：dispatch shell 失败 + withRetry(3)，验证重试至多 3 次
  - [ ] SubTask 56.4: cron 任务：tests/cron.phpt 验证 cron 触发
  - [ ] SubTask 56.5: overlap 任务：慢任务 + allowOverlap(false)，验证排队执行
  - [ ] SubTask 56.6: persist 任务：persist(true) + 投递，restart daemon 后验证任务状态可查
  - [ ] SubTask 56.7: 多服务：两个服务 PID 不同且互不干扰
  - [ ] SubTask 56.8: maxExecutions：cron + maxExecutions(3)，验证执行 3 次后停止（**验证 bug 修复**）
  - [ ] SubTask 56.9: lifecycle：pause/resume/cancel/remove/list 全部按预期
  - [ ] SubTask 56.10: start_end_date：startAt/endAt 时间窗口生效
  - [ ] SubTask 56.11: result_ttl：result 过期清理
  - [ ] SubTask 56.12: meta：meta 字段读写
  - [ ] SubTask 56.13: priority：高优先级先执行
  - [ ] SubTask 56.14: interval (A7)：every(2) 周期触发验证
  - [ ] SubTask 56.15: runAt (A8)：一次性绝对时刻触发验证
  - [ ] SubTask 56.16: jitter (A9)：多任务触发时刻分散验证
  - [ ] SubTask 56.17: expires (C6)：超时 Pending 任务置 Expired 验证
  - [ ] SubTask 56.18: requeue (C7)：cancel → requeue → 重新触发验证
  - [ ] SubTask 56.19: retry_backoff (C8)：retryBackoff(true) 指数退避时间序列验证
  - [ ] SubTask 56.20: max_instances (A10)：maxInstances(2) 允许 2 个并发实例 + 第 3 个被跳过验证
  - [ ] SubTask 56.21: reschedule (A11)：reschedule 修改 cron + 保留 execution_count 验证
  - [ ] SubTask 56.22: get_job (A12)：xhjob_get 返回完整 Task JSON 字段验证
  - [ ] SubTask 56.23: ignore_result (C9)：ignoreResult(true) 不存 result + state 仍正常验证
  - [ ] SubTask 56.24: acks_late (C10)：daemon 重启后 acks_late 任务被重排验证
  - [ ] SubTask 56.25: soft_timeout (C11)：softTimeout(5)+timeout(10) shell 任务 SIGTERM 优雅退出验证
  - [ ] SubTask 56.26: misfire_grace_time (A13)：misfireGraceTime(5) 短窗口跳过 6s 延迟 + 默认 0 用全局 60s 验证
  - [ ] SubTask 56.27: replace_existing (A14)：withId + replaceExisting(true) 同 id 重跑覆盖 + 默认 false 报错验证
  - [ ] SubTask 56.28: tags (A15)：tags 标记 + xhjob_list 按 tag 过滤 + 默认空数组不返回验证
  - [ ] SubTask 56.29: rate_limit (C12)：rateLimit(3, 10) 滑动窗口限流 + 第 11 秒窗口滑过允许验证
  - [ ] SubTask 56.30: acks_on_failure (C13)：acksOnFailure(false) 失败任务持续重试忽略 retry_max + 被 cancel 终止验证
  - [ ] SubTask 56.31: max_tasks_per_child (C14)：XHJOB_MAX_TASKS_PER_CHILD=5 daemon 执行 5 个任务后优雅退出验证

- [ ] Task 57: 提交并推送远程主分支（**取代 Task 46**，含 18 个新对齐功能改动：6 项 round 2 + 6 项 round 3 + 6 项 round 4）（**已废弃，由 Task 68 取代**）
  - [ ] SubTask 57.1: `git status` 核对修改文件清单
  - [ ] SubTask 57.2: `git diff` 审查改动内容（确认无意外改动、无删除用户文件）
  - [ ] SubTask 57.3: `git add <指定文件>` 暂存改动（不 `git add -A`，避免误加 spec 文档外文件）
  - [ ] SubTask 57.4: `git commit -m "feat: 对齐 APScheduler/Celery 29 项（A1-A15+C1-C14）+ maxExecutions bug 修复 + 18 项新对齐功能（every/runAt/jitter/expires/requeue/retryBackoff/maxInstances升级/reschedule/get/ignoreResult/acksLate/softTimeout/misfireGraceTime/replaceExisting/tags/rateLimit/acksOnFailure/maxTasksPerChild）+ 文档/示例/测试补齐"` 提交到本地 main
  - [ ] SubTask 57.5: `git push origin main` 推送到远程主分支
  - [ ] SubTask 57.6: `git log origin/main --oneline -5` 确认远程 HEAD 已更新

- [x] Task 58: 新功能 A16 — timezone per-job（每作业独立时区，对齐 APScheduler `CronTrigger(timezone=...)`）
  - [ ] SubTask 58.1: `Cargo.toml` 新增 `chrono-tz = "0.9"` 依赖
  - [ ] SubTask 58.2: `Task` struct 新增 `timezone: Option<String>` 字段（默认 None = 使用 daemon 全局时区，向后兼容），加 `#[serde(default)]`
  - [ ] SubTask 58.3: `Task::new` 初始化为 None
  - [ ] SubTask 58.4: `TaskBuilder` 新增 `timezone: Option<String>` 字段 + builder 方法 `timezone(tz: impl Into<String>)` + `build()` 复制
  - [ ] SubTask 58.5: `Xhjob` 类新增 `timezone(&mut self, tz: String) -> &mut Self`，PHP 暴露为 `timezone(string $tz): $this`
  - [ ] SubTask 58.6: `xhjob_dispatch` 透传 `timezone` 字段到 task JSON
  - [ ] SubTask 58.7: `scan_once` 中 cron 表达式评估时：`let tz = task.timezone.as_ref().and_then(|s| s.parse::<chrono_tz::Tz>().ok()).unwrap_or(global_timezone);`，解析失败时 `tracing::warn!("invalid timezone {}, fallback to global", tz)` 并回退全局
  - [ ] SubTask 58.8: 非 cron 任务（interval / runAt）设置 `timezone` 时 `tracing::warn!("timezone is only effective for cron tasks, ignored")` 并忽略
  - [ ] SubTask 58.9: `sqlite.rs` schema 新增 `timezone TEXT` 列（旧库 ALTER TABLE 兼容）
  - [ ] SubTask 58.10: `sqlite.rs::task_from_row` / `insert_task` 同步读写新字段
  - [ ] SubTask 58.11: `StateInfo` 新增 `timezone` 字段 + `xhjob_state` 返回
  - [ ] SubTask 58.12: 单元测试 `test_timezone_per_job_overrides_global`：global=UTC + task.timezone="America/New_York" → cron 表达式按纽约时区评估 next_fire
  - [ ] SubTask 58.13: 单元测试 `test_timezone_invalid_falls_back_to_global`：task.timezone="Invalid/Zone" → warn + 回退全局
  - [ ] SubTask 58.14: 单元测试 `test_timezone_default_none_uses_global`：默认 None → 使用全局时区（向后兼容）
  - [ ] SubTask 58.15: 单元测试 `test_timezone_ignored_for_interval_tasks`：interval + timezone → warn + 忽略
  - [ ] SubTask 58.16: `tests/timezone_test.php` 端到端验证 timezone('America/New_York') cron 任务按纽约时区评估

- [x] Task 59: 新功能 A17 — Event listener API（任务执行事件流查询，对齐 APScheduler `add_listener` + `EVENT_JOB_*`）
  - [ ] SubTask 59.1: 新增 `src/scheduler/events.rs` 模块，定义 `pub struct TaskEvent { task_id: String, event_type: EventType, payload: Option<String>, ts: i64 }`
  - [ ] SubTask 59.2: 定义 `pub enum EventType { Started, Succeeded, Failed, Missed, Cancelled, Paused, Resumed, Expired, MaxInstancesReached, RateLimited }` + `as_str()` / `from_str()` 实现
  - [ ] SubTask 59.3: `TaskStore` trait 新增 `fn record_event(&self, task_id: &str, event_type: EventType, payload: Option<&str>, ts: i64) -> Result<()>`
  - [ ] SubTask 59.4: `TaskStore` trait 新增 `fn list_events(&self, since_ts: i64, task_id_filter: Option<&str>) -> Result<Vec<TaskEvent>>`
  - [ ] SubTask 59.5: `TaskStore` trait 新增 `fn cleanup_expired_events(&self, ttl_secs: u64) -> Result<u64>`
  - [ ] SubTask 59.6: `in_memory.rs` 实现 3 个新方法（用 `Vec<TaskEvent>` + RwLock）
  - [ ] SubTask 59.7: `sqlite.rs` schema 新增 `events` 表（`id INTEGER PRIMARY KEY AUTOINCREMENT` / `task_id TEXT NOT NULL` / `event_type TEXT NOT NULL` / `payload TEXT` / `ts INTEGER NOT NULL`）+ 索引 `idx_events_ts` ON events(ts)
  - [ ] SubTask 59.8: `sqlite.rs` 实现 3 个新方法（INSERT / SELECT WHERE ts >= ? AND (? IS NULL OR task_id = ?) ORDER BY ts / DELETE WHERE ts < ?）
  - [ ] SubTask 59.9: `process_one` 中关键路径调用 `record_event`：任务开始时 EventType::Started，成功时 Succeeded，失败时 Failed
  - [ ] SubTask 59.10: `scan_once` 中调用 `record_event`：misfire 时 Missed，max_instances_reached 时 MaxInstancesReached，rate_limited 时 RateLimited
  - [ ] SubTask 59.11: `cancel_task` / `set_paused` (true/false) / 标记 Expired 时分别调用 record_event 写入 Cancelled / Paused / Resumed / Expired
  - [ ] SubTask 59.12: `scan_once` 周期性调用 `cleanup_expired_events`（每 60s 节流，static AtomicU64，TTL 从 `XHJOB_EVENTS_TTL_SECS` 环境变量读取，默认 86400）
  - [ ] SubTask 59.13: `daemon_main.rs::Config` 新增 `events_ttl_secs: u64` 字段（从环境变量读取，默认 86400）
  - [ ] SubTask 59.14: `lib.rs` 新增 PHP 顶层函数 `xhjob_events($since_ts, $name='default', $task_id=null, $data_dir=null): string` 返回 JSON 数组
  - [ ] SubTask 59.15: `lib.rs` 新增 IPC 命令 `events` 路由 + `daemon_main.rs` 新增 `handle_events_op` handler
  - [ ] SubTask 59.16: 单元测试 `test_record_event_persists`：record_event Started → list_events 返回 1 条
  - [ ] SubTask 59.17: 单元测试 `test_list_events_filter_by_task_id`：3 个任务各 record 1 事件 → list_events(task_id=X) 返回 1 条
  - [ ] SubTask 59.18: 单元测试 `test_list_events_since_ts_filter`：3 事件 ts=[10,20,30] → list_events(since_ts=20) 返回 2 条
  - [ ] SubTask 59.19: 单元测试 `test_cleanup_expired_events`：插入 3 事件 ts=[now-100, now-50, now-10] → cleanup(60) 删除前 2 条，返回 2
  - [ ] SubTask 59.20: 单元测试 `test_event_types_cover_all_paths`：模拟 started / succeeded / failed / missed / cancelled / paused / resumed / expired / max_instances_reached / rate_limited → list_events 返回 10 条覆盖全部类型
  - [ ] SubTask 59.21: `tests/events_test.php` 端到端验证 xhjob_events 查询任务执行事件流 + TTL 清理

- [x] Task 60: 新功能 A18 — coalesce 显式 per-job 行为（misfire 合并/丢弃规则真正生效）
  - [ ] SubTask 60.1: 确认 `Task` struct 已有 `coalesce: bool` 字段（默认 true 保持当前行为），如未存在则补充
  - [ ] SubTask 60.2: `TaskBuilder` 新增 `coalesce(on: bool)` builder 方法（如未存在）
  - [ ] SubTask 60.3: `Xhjob` 类新增 `coalesce(&mut self, on: bool) -> &mut Self`，PHP 暴露为 `coalesce(bool $on): $this`（如未存在）
  - [ ] SubTask 60.4: `scan_once` 中 misfire 检测时（配合 A13 `misfire_grace_time`）真正按 `task.coalesce` 字段控制行为：
    - `coalesce=true`（默认）：合并多次 missed 触发为最后一次执行（dispatch 一次）+ 推进 next_fire 至下一轮 cron 时刻
    - `coalesce=false`：丢弃所有 missed 触发（不执行任何补偿）+ 推进 next_fire 至下一轮 cron 时刻
  - [ ] SubTask 60.5: `coalesce=true` 时 execution_count 仅递增 1（而非 N，N=missed 次数）
  - [ ] SubTask 60.6: `coalesce=false` 时 execution_count 不递增（不执行）
  - [ ] SubTask 60.7: `coalesce` 默认 true 保持当前行为（向后兼容）
  - [ ] SubTask 60.8: `coalesce` 与 `misfire_grace_time` 配合：`coalesce=false` + `misfireGraceTime(5)` + 6 秒停顿 → 完全 misfire，按 `coalesce=false` 丢弃所有 missed 触发并推进 next_fire，不执行任何补偿
  - [ ] SubTask 60.9: `coalesce=true` + `misfireGraceTime(5)` + 6 秒停顿：完全 misfire，但仍按合并规则执行最后一次
  - [ ] SubTask 60.10: `sqlite.rs` schema 新增 `coalesce INTEGER NOT NULL DEFAULT 1` 列（默认 1=true，旧库 ALTER TABLE 兼容）
  - [ ] SubTask 60.11: `sqlite.rs::task_from_row` / `insert_task` 同步读写新字段
  - [ ] SubTask 60.12: `StateInfo` 新增 `coalesce` 字段 + `xhjob_state` 返回
  - [ ] SubTask 60.13: 单元测试 `test_coalesce_true_merges_missed_into_last_execution`：cron('*/1 * * * * *')->coalesce(true)->misfireGraceTime(60) + GC 停顿 5 秒 → 合并 5 次 missed 为最后一次执行，execution_count 仅 +1
  - [ ] SubTask 60.14: 单元测试 `test_coalesce_false_drops_missed_no_compensation`：cron('*/1 * * * * *')->coalesce(false)->misfireGraceTime(60) + GC 停顿 5 秒 → 丢弃 5 次 missed，execution_count 不递增
  - [ ] SubTask 60.15: 单元测试 `test_coalesce_default_true_backwards_compatible`：默认 true 时行为与当前一致
  - [ ] SubTask 60.16: 单元测试 `test_coalesce_with_misfire_grace_time_full_misfire`：coalesce(false)+misfireGraceTime(5)+6 秒停顿 → 完全 misfire 丢弃
  - [ ] SubTask 60.17: `tests/coalesce_test.php` 端到端验证 coalesce(true) 合并 missed / coalesce(false) 丢弃 missed 行为

- [x] Task 61: 新功能 C15 — Task chain 顺序流水线（对齐 Celery `chain`）
  - [ ] SubTask 61.1: 新增 `src/scheduler/chain.rs` 模块，定义 `pub struct ChainExecutor`，负责推进 chain 的 current_step
  - [ ] SubTask 61.2: `TaskStore` trait 新增 `fn create_chain(&self, chain_id: &str, tasks: &[serde_json::Value], created_at: i64) -> Result<()>`
  - [ ] SubTask 61.3: `TaskStore` trait 新增 `fn get_chain(&self, chain_id: &str) -> Result<Option<ChainRecord>>`
  - [ ] SubTask 61.4: `TaskStore` trait 新增 `fn update_chain_step(&self, chain_id: &str, current_step: u32, state: &str, updated_at: i64) -> Result<()>`
  - [ ] SubTask 61.5: `TaskStore` trait 新增 `fn list_chains_by_state(&self, state: &str) -> Result<Vec<ChainRecord>>`（用于 daemon 重启后恢复未完成 chain）
  - [ ] SubTask 61.6: `in_memory.rs` 与 `sqlite.rs` 实现新方法
  - [ ] SubTask 61.7: `sqlite.rs` schema 新增 `chains` 表（`chain_id TEXT PRIMARY KEY` / `tasks TEXT NOT NULL` JSON 数组 / `current_step INTEGER NOT NULL DEFAULT 0` / `state TEXT NOT NULL DEFAULT 'pending'` / `created_at INTEGER NOT NULL` / `updated_at INTEGER NOT NULL`）
  - [ ] SubTask 61.8: daemon 启动时调用 `list_chains_by_state("pending")` 与 `list_chains_by_state("running")` 恢复未完成 chain
  - [ ] SubTask 61.9: daemon 接收到 `chain` 命令后：生成 chain_id (UUID) → 调用 create_chain → dispatch 第 1 个任务
  - [ ] SubTask 61.10: `process_one` 任务完成后，检查任务是否属于某个 chain（通过 task meta 字段或 chain_id 标识），若属于 chain：
    - 任务 success：取 stdout 作为下一任务的 input payload → dispatch 下一任务 → update_chain_step(current_step+1, "running")
    - 任务 failed：update_chain_step(state="failed") → 剩余任务不再 dispatch
  - [ ] SubTask 61.11: 最后一任务 success 后 update_chain_step(state="succeeded")
  - [ ] SubTask 61.12: 前任务 stdout 通过 `XHJOB_CHAIN_INPUT` 环境变量传入后任务（shell 任务通过 stdin / 环境变量读取；HTTP 任务作为 body payload）
  - [ ] SubTask 61.13: `lib.rs` 新增 PHP 顶层函数 `xhjob_chain(array $task_configs, $name='default', $data_dir=null): string` 返回 chain_id
  - [ ] SubTask 61.14: `lib.rs` 新增 PHP 顶层函数 `xhjob_chain_state($chain_id, $name='default', $data_dir=null): string` 返回 JSON（含 chain_id / tasks / current_step / state）
  - [ ] SubTask 61.15: `lib.rs` 新增 IPC 命令 `chain` / `chain_state` 路由 + `daemon_main.rs` 新增 `handle_chain_op` / `handle_chain_state_op` handler
  - [ ] SubTask 61.16: 空 tasks 数组返回 `error: chain tasks cannot be empty`
  - [ ] SubTask 61.17: 单元测试 `test_chain_three_steps_etl_pipeline`：3 步 chain extract → transform → load，每步 stdout 作为下步 stdin
  - [ ] SubTask 61.18: 单元测试 `test_chain_failure_stops_pipeline`：第 2 步失败 → chain state=failed，current_step 停在 1，第 3 步不 dispatch
  - [ ] SubTask 61.19: 单元测试 `test_chain_state_query_returns_full_progress`：3 步 chain + 1 步完成 → xhjob_chain_state 返回 {state:"running", current_step:1, tasks:[{success},{pending},{pending}]}
  - [ ] SubTask 61.20: 单元测试 `test_chain_persists_across_daemon_restart`：第 1 步 success 后 daemon 崩溃重启 → 从 chains 表恢复 + 继续 dispatch 第 2 步
  - [ ] SubTask 61.21: 单元测试 `test_chain_empty_tasks_returns_error`：xhjob_chain([]) → 返回 `error: chain tasks cannot be empty`
  - [ ] SubTask 61.22: `tests/chain_test.php` 端到端验证 xhjob_chain 顺序流水线 + 前任务输出作为后任务输入 + 失败中断

- [x] Task 62: 新功能 C16 — Task group 并行批处理（对齐 Celery `group`）
  - [ ] SubTask 62.1: 新增 `src/scheduler/group.rs` 模块，定义 `pub struct GroupWatcher`，监听 group 内任务终态并更新 group state
  - [ ] SubTask 62.2: `TaskStore` trait 新增 `fn create_group(&self, group_id: &str, tasks: &[serde_json::Value], created_at: i64) -> Result<()>`
  - [ ] SubTask 62.3: `TaskStore` trait 新增 `fn get_group(&self, group_id: &str) -> Result<Option<GroupRecord>>`
  - [ ] SubTask 62.4: `TaskStore` trait 新增 `fn update_group_state(&self, group_id: &str, state: &str, updated_at: i64) -> Result<()>`
  - [ ] SubTask 62.5: `TaskStore` trait 新增 `fn list_groups_by_state(&self, state: &str) -> Result<Vec<GroupRecord>>`（用于 daemon 重启后恢复未完成 group）
  - [ ] SubTask 62.6: `in_memory.rs` 与 `sqlite.rs` 实现新方法
  - [ ] SubTask 62.7: `sqlite.rs` schema 新增 `groups` 表（`group_id TEXT PRIMARY KEY` / `tasks TEXT NOT NULL` JSON 数组 / `state TEXT NOT NULL DEFAULT 'pending'` / `created_at INTEGER NOT NULL` / `updated_at INTEGER NOT NULL`）
  - [ ] SubTask 62.8: daemon 启动时调用 `list_groups_by_state("pending")` 与 `list_groups_by_state("running")` 恢复未完成 group
  - [ ] SubTask 62.9: daemon 接收到 `group` 命令后：生成 group_id (UUID) → 调用 create_group → 并行 dispatch 所有任务（同时入队，按 priority 排队）
  - [ ] SubTask 62.10: `process_one` 任务完成后，检查任务是否属于某个 group（通过 task meta 字段或 group_id 标识），若属于 group：
    - 任务 success：completed_count += 1
    - 任务 failed：completed_count += 1
    - 全部任务终态后：判断 group state（全部 success → succeeded / 部分失败 → partial_failed / 全部失败 → failed）→ update_group_state
  - [ ] SubTask 62.11: `lib.rs` 新增 PHP 顶层函数 `xhjob_group(array $task_configs, $name='default', $data_dir=null): string` 返回 group_id
  - [ ] SubTask 62.12: `lib.rs` 新增 PHP 顶层函数 `xhjob_group_state($group_id, $name='default', $data_dir=null): string` 返回 JSON（含 group_id / tasks / completed_count / total_count / state）
  - [ ] SubTask 62.13: `lib.rs` 新增 IPC 命令 `group` / `group_state` 路由 + `daemon_main.rs` 新增 `handle_group_op` / `handle_group_state_op` handler
  - [ ] SubTask 62.14: 空 tasks 数组返回 `error: group tasks cannot be empty`
  - [ ] SubTask 62.15: group 内任务受 max_instances / rate_limit 限制（与单任务一致）
  - [ ] SubTask 62.16: 单元测试 `test_group_three_tasks_parallel_dispatch`：3 任务 group → 同时入队 → 全部 success → state=succeeded
  - [ ] SubTask 62.17: 单元测试 `test_group_partial_failure`：3 任务 group + 1 失败 → state=partial_failed，completed_count=3
  - [ ] SubTask 62.18: 单元测试 `test_group_all_failed`：3 任务 group + 全部失败 → state=failed
  - [ ] SubTask 62.19: 单元测试 `test_group_state_query_returns_progress`：3 任务 group + 2 完成 → xhjob_group_state 返回 {state:"running", completed_count:2, total_count:3}
  - [ ] SubTask 62.20: 单元测试 `test_group_persists_across_daemon_restart`：3 任务 group + 2 success 后 daemon 崩溃重启 → 从 groups 表恢复 + 等待第 3 任务终态
  - [ ] SubTask 62.21: 单元测试 `test_group_empty_tasks_returns_error`：xhjob_group([]) → 返回 `error: group tasks cannot be empty`
  - [ ] SubTask 62.22: `tests/group_test.php` 端到端验证 xhjob_group 并行批处理 + group_state 完成率

- [x] Task 63: 新功能 C17 — worker_max_memory_per_child 基于内存的 daemon 自我回收
  - [ ] SubTask 63.1: `daemon_main.rs::Config` 新增 `max_memory_per_child: u64` 字段（从环境变量 `XHJOB_MAX_MEMORY_PER_CHILD` 读取，默认 0=不回收，单位 MB）
  - [ ] SubTask 63.2: 新增 `src/utils/memory.rs` 模块（或扩展 `daemon_main.rs`），实现跨平台 `pub fn read_process_memory_mb() -> u64`：
    - Linux：读取 `/proc/self/status` 中 `VmRSS:` 行，解析 KB 单位值，转换为 MB
    - macOS：使用 `mach_task_basic_info` API（通过 `sysctl` crate 或 `libc`）
    - Windows：使用 `GetProcessMemoryInfo` API（通过 `winapi` crate）
    - 其他平台：返回 0 + warn 不支持
  - [ ] SubTask 63.3: `src/utils/mod.rs` 注册 `pub mod memory;` + `pub use memory::read_process_memory_mb;`
  - [ ] SubTask 63.4: `Cargo.toml` 新增平台特定依赖：Linux 无需新增 / macOS `sysctl = "0.5"` / Windows `winapi = { version = "0.3", features = ["psapi", "winnt"] }`（用 `[target.'cfg(...)'.dependencies]`）
  - [ ] SubTask 63.5: `process_one` 完成后（无论成功失败）调用 `read_process_memory_mb()` 获取当前内存使用
  - [ ] SubTask 63.6: 检查 `if max_memory_per_child > 0 && vm_rss_mb >= max_memory_per_child`：触发优雅退出流程（与 C14 复用同一退出函数）
  - [ ] SubTask 63.7: 优雅退出流程：通知 scheduler 停止接受新触发 / 通知 queue 等待 in-flight 任务完成 / flush store / log INFO "max_memory_per_child reached ({}MB >= {}MB), exiting" / exit 0
  - [ ] SubTask 63.8: `max_memory_per_child=0`（默认）时不检查不退出，行为与当前一致（向后兼容）
  - [ ] SubTask 63.9: 与 C14 `max_tasks_per_child` 互补：两个检查独立生效，先到达阈值者先触发退出
  - [ ] SubTask 63.10: daemon 重启后内存计数从当前 VmRSS 开始（不持久化，仅内存检查）
  - [ ] SubTask 63.11: 优雅退出等待 in-flight 任务完成（不强制 kill Running 任务），最长等待 timeout 秒后强制退出
  - [ ] SubTask 63.12: 单元测试 `test_max_memory_per_child_triggers_exit_when_exceeded`：max_memory_per_child=100 + 模拟 VmRSS=150MB → daemon 退出（mock read_process_memory_mb 返回固定值）
  - [ ] SubTask 63.13: 单元测试 `test_max_memory_per_child_default_0_no_exit`：默认 0 时不受内存限制（向后兼容）
  - [ ] SubTask 63.14: 单元测试 `test_max_memory_per_child_independent_from_max_tasks`：max_tasks=1000 + max_memory=100 + 任务数=500 + VmRSS=200MB → 内存先触发退出
  - [ ] SubTask 63.15: 单元测试 `test_read_process_memory_mb_returns_positive_on_linux`：在 Linux 平台 read_process_memory_mb 返回 > 0
  - [ ] SubTask 63.16: 单元测试 `test_max_memory_per_child_waits_inflight`：max=100 + VmRSS=200MB + 2 个 Running 任务 → daemon 等待任务完成后才退出
  - [ ] SubTask 63.17: `tests/max_memory_per_child_test.php` 端到端验证 XHJOB_MAX_MEMORY_PER_CHILD=100 daemon 内存超阈值后优雅退出

- [x] Task 64: README + examples 文档更新（A16-A18 + C15-C17 第五轮对齐项）
  - [ ] SubTask 64.1: Xhjob 类方法表新增 `timezone(string $tz): $this` / `coalesce(bool $on): $this` 2 行
  - [ ] SubTask 64.2: 顶层函数表新增 `xhjob_events($since_ts, $name='default', $task_id=null, $data_dir=null): string` / `xhjob_chain(array $task_configs, $name='default', $data_dir=null): string` / `xhjob_chain_state($chain_id, $name='default', $data_dir=null): string` / `xhjob_group(array $task_configs, $name='default', $data_dir=null): string` / `xhjob_group_state($group_id, $name='default', $data_dir=null): string` 5 行
  - [ ] SubTask 64.3: `xhjob_state` 返回说明新增 `timezone` / `coalesce` 字段
  - [ ] SubTask 64.4: 环境变量表新增 `XHJOB_MAX_MEMORY_PER_CHILD` 行说明"daemon 内存超 N MB 后自我回收，与 max_tasks_per_child 互补；默认 0=不回收"
  - [ ] SubTask 64.5: 环境变量表新增 `XHJOB_EVENTS_TTL_SECS` 行说明"events 表 TTL 自动清理，默认 86400（24h）"
  - [ ] SubTask 64.6: 新增"timezone per-job 每作业独立时区（A16）"小节：说明 IANA 时区标识 + 解析失败回退全局 + 非 cron 任务忽略 + 跨国应用场景
  - [ ] SubTask 64.7: 新增"Event listener API 任务执行事件流（A17）"小节：说明 xhjob_events 查询 + 事件类型清单 + TTL 清理 + 监控集成场景
  - [ ] SubTask 64.8: 新增"coalesce 显式 per-job 行为（A18）"小节：说明 coalesce(true) 合并 missed / coalesce(false) 丢弃 missed + 与 misfireGraceTime 配合 + 适用场景
  - [ ] SubTask 64.9: 新增"Task chain 顺序流水线（C15）"小节：说明 xhjob_chain + 前任务输出作为后任务输入 + 失败中断 + 持久化 + ETL 管道场景
  - [ ] SubTask 64.10: 新增"Task group 并行批处理（C16）"小节：说明 xhjob_group + 并行 dispatch + group_state 完成率 + 批量处理场景
  - [ ] SubTask 64.11: 新增"worker_max_memory_per_child 基于内存 daemon 自我回收（C17）"小节：说明环境变量配置 + 跨平台内存读取 + 与 max_tasks_per_child 互补 + 内存泄漏场景
  - [ ] SubTask 64.12: "对照 APScheduler / Celery 的功能对齐"小节扩展为 35 项（A1-A18 + C1-C17）
  - [ ] SubTask 64.13: 新增 `examples/cron_timezone.php`：演示 timezone('America/New_York') cron 任务按纽约时区评估（A16）
  - [ ] SubTask 64.14: 新增 `examples/events_query.php`：演示 xhjob_events 查询过去 1 小时任务执行事件流（A17）
  - [ ] SubTask 64.15: 新增 `examples/chain_etl.php`：演示 xhjob_chain 3 步 ETL 流水线 extract → transform → load（C15）
  - [ ] SubTask 64.16: 新增 `examples/group_batch.php`：演示 xhjob_group 3 任务并行批处理 + group_state 查询完成率（C16）

- [x] Task 65: 新增 6 个第五轮对齐测试文件（A16-A18 + C15-C17）
  - [ ] SubTask 65.1: `tests/timezone_test.php` 验证 timezone('America/New_York') cron 任务按纽约时区评估 + 解析失败回退全局 + 非 cron 任务忽略（PASS）
  - [ ] SubTask 65.2: `tests/events_test.php` 验证 xhjob_events 查询任务执行事件流 + 按 task_id 过滤 + 按 since_ts 过滤 + TTL 清理（PASS）
  - [ ] SubTask 65.3: `tests/coalesce_test.php` 验证 coalesce(true) 合并 missed 触发为最后一次执行 + coalesce(false) 丢弃 missed 不执行补偿 + 默认 true 向后兼容（PASS）
  - [ ] SubTask 65.4: `tests/chain_test.php` 验证 xhjob_chain 3 步 ETL 流水线 + 前任务输出作为后任务输入 + 失败中断 + 持久化（PASS）
  - [ ] SubTask 65.5: `tests/group_test.php` 验证 xhjob_group 3 任务并行批处理 + group_state 完成率 + partial_failed 状态 + 持久化（PASS）
  - [ ] SubTask 65.6: `tests/max_memory_per_child_test.php` 验证 XHJOB_MAX_MEMORY_PER_CHILD=100 daemon 内存超阈值后优雅退出 + 默认 0 不退出 + 与 max_tasks_per_child 互补（PASS）

- [x] Task 66: 重新编译 + 重跑全量测试（含 24 个新对齐测试：6 项 round 2 + 6 项 round 3 + 6 项 round 4 + 6 项 round 5，**取代 Task 55**）
  - [ ] SubTask 66.1: `cargo build --release --features persist` 退出码 0 且无 warning
  - [ ] SubTask 66.2: `cargo build --release`（默认 feature）退出码 0 且无 warning
  - [ ] SubTask 66.3: `cargo test --release --lib --features persist` 全部通过（含 maxExecutions bug 修复 + 24 个新功能单元测试）
  - [ ] SubTask 66.4: `cargo test --release --lib`（默认 feature）全部通过
  - [ ] SubTask 66.5: 重新部署 `libxhjob.so` 到 `php-config --extension-dir`
  - [ ] SubTask 66.6: `php run-tests.php tests/` 全量 .phpt 仍 7 PASS / 3 SKIP / 0 FAIL
  - [ ] SubTask 66.7: `php tests/boundary_cases.php` 全部 PASS（4xx 测试可 SKIP）
  - [ ] SubTask 66.8: `php tests/max_executions.php` 全部 PASS（**验证 bug 修复**）
  - [ ] SubTask 66.9: `php tests/lifecycle_api.php` 全部 PASS
  - [ ] SubTask 66.10: `php tests/start_end_date.php` 全部 PASS
  - [ ] SubTask 66.11: `php tests/result_ttl.php` 全部 PASS
  - [ ] SubTask 66.12: `php tests/meta_field.php` 全部 PASS
  - [ ] SubTask 66.13: `php tests/interval_trigger.php` 全部 PASS（A7）
  - [ ] SubTask 66.14: `php tests/run_at_trigger.php` 全部 PASS（A8）
  - [ ] SubTask 66.15: `php tests/jitter_test.php` 全部 PASS（A9）
  - [ ] SubTask 66.16: `php tests/expires_test.php` 全部 PASS（C6）
  - [ ] SubTask 66.17: `php tests/requeue_test.php` 全部 PASS（C7）
  - [ ] SubTask 66.18: `php tests/retry_backoff.php` 全部 PASS（C8，无网络则 SKIP）
  - [ ] SubTask 66.19: `php tests/max_instances_test.php` 全部 PASS（A10）
  - [ ] SubTask 66.20: `php tests/reschedule_test.php` 全部 PASS（A11）
  - [ ] SubTask 66.21: `php tests/get_job_test.php` 全部 PASS（A12）
  - [ ] SubTask 66.22: `php tests/ignore_result_test.php` 全部 PASS（C9）
  - [ ] SubTask 66.23: `php tests/acks_late_test.php` 全部 PASS（C10）
  - [ ] SubTask 66.24: `php tests/soft_timeout_test.php` 全部 PASS（C11）
  - [ ] SubTask 66.25: `php tests/misfire_grace_time_test.php` 全部 PASS（A13）
  - [ ] SubTask 66.26: `php tests/replace_existing_test.php` 全部 PASS（A14）
  - [ ] SubTask 66.27: `php tests/tags_test.php` 全部 PASS（A15）
  - [ ] SubTask 66.28: `php tests/rate_limit_test.php` 全部 PASS（C12）
  - [ ] SubTask 66.29: `php tests/acks_on_failure_test.php` 全部 PASS（C13）
  - [ ] SubTask 66.30: `php tests/max_tasks_per_child_test.php` 全部 PASS（C14）
  - [ ] SubTask 66.31: `php tests/timezone_test.php` 全部 PASS（A16）
  - [ ] SubTask 66.32: `php tests/events_test.php` 全部 PASS（A17）
  - [ ] SubTask 66.33: `php tests/coalesce_test.php` 全部 PASS（A18）
  - [ ] SubTask 66.34: `php tests/chain_test.php` 全部 PASS（C15）
  - [ ] SubTask 66.35: `php tests/group_test.php` 全部 PASS（C16）
  - [ ] SubTask 66.36: `php tests/max_memory_per_child_test.php` 全部 PASS（C17）
  - [ ] SubTask 66.37: `bash tests/business/cli_bus/run_all.sh` 4 步全部 PASS
  - [ ] SubTask 66.38: `php tests/business/fpm_sim/proc_test.php` 10 步全部 PASS
  - [ ] SubTask 66.39: `php tests/business/fpm_sim/client_test.php` 10 步全部 PASS
  - [ ] SubTask 66.40: `php tests/data_dir_smoke.php` PASS
  - [ ] SubTask 66.41: 20 个 `examples/*.php`（含新增 cron_timezone / events_query / chain_etl / group_batch）全部可运行（无 fatal error）
  - [ ] SubTask 66.42: `php tests/functional_verify.php` 15 步全部 PASS（验证既有功能未回归）

- [x] Task 67: 功能模块独立验证（含 24 个新对齐功能：6 项 round 2 + 6 项 round 3 + 6 项 round 4 + 6 项 round 5，**取代 Task 56**）
  - [ ] SubTask 67.1: shell 任务：dispatch echo 命令，验证 stdout 与 exit_code（PASS）
  - [ ] SubTask 67.2: retry 任务：dispatch HTTP 404 + withRetry(3)，验证不重试 attempts=1
  - [ ] SubTask 67.3: retry 任务：dispatch shell 失败 + withRetry(3)，验证重试至多 3 次
  - [ ] SubTask 67.4: cron 任务：tests/cron.phpt 验证 cron 触发
  - [ ] SubTask 67.5: overlap 任务：慢任务 + allowOverlap(false)，验证排队执行
  - [ ] SubTask 67.6: persist 任务：persist(true) + 投递，restart daemon 后验证任务状态可查
  - [ ] SubTask 67.7: 多服务：两个服务 PID 不同且互不干扰
  - [ ] SubTask 67.8: maxExecutions：cron + maxExecutions(3)，验证执行 3 次后停止（**验证 bug 修复**）
  - [ ] SubTask 67.9: lifecycle：pause/resume/cancel/remove/list 全部按预期
  - [ ] SubTask 67.10: start_end_date：startAt/endAt 时间窗口生效
  - [ ] SubTask 67.11: result_ttl：result 过期清理
  - [ ] SubTask 67.12: meta：meta 字段读写
  - [ ] SubTask 67.13: priority：高优先级先执行
  - [ ] SubTask 67.14: interval (A7)：every(2) 周期触发验证
  - [ ] SubTask 67.15: runAt (A8)：一次性绝对时刻触发验证
  - [ ] SubTask 67.16: jitter (A9)：多任务触发时刻分散验证
  - [ ] SubTask 67.17: expires (C6)：超时 Pending 任务置 Expired 验证
  - [ ] SubTask 67.18: requeue (C7)：cancel → requeue → 重新触发验证
  - [ ] SubTask 67.19: retry_backoff (C8)：retryBackoff(true) 指数退避时间序列验证
  - [ ] SubTask 67.20: max_instances (A10)：maxInstances(2) 允许 2 个并发实例 + 第 3 个被跳过验证
  - [ ] SubTask 67.21: reschedule (A11)：reschedule 修改 cron + 保留 execution_count 验证
  - [ ] SubTask 67.22: get_job (A12)：xhjob_get 返回完整 Task JSON 字段验证
  - [ ] SubTask 67.23: ignore_result (C9)：ignoreResult(true) 不存 result + state 仍正常验证
  - [ ] SubTask 67.24: acks_late (C10)：daemon 重启后 acks_late 任务被重排验证
  - [ ] SubTask 67.25: soft_timeout (C11)：softTimeout(5)+timeout(10) shell 任务 SIGTERM 优雅退出验证
  - [ ] SubTask 67.26: misfire_grace_time (A13)：misfireGraceTime(5) 短窗口跳过 6s 延迟 + 默认 0 用全局 60s 验证
  - [ ] SubTask 67.27: replace_existing (A14)：withId + replaceExisting(true) 同 id 重跑覆盖 + 默认 false 报错验证
  - [ ] SubTask 67.28: tags (A15)：tags 标记 + xhjob_list 按 tag 过滤 + 默认空数组不返回验证
  - [ ] SubTask 67.29: rate_limit (C12)：rateLimit(3, 10) 滑动窗口限流 + 第 11 秒窗口滑过允许验证
  - [ ] SubTask 67.30: acks_on_failure (C13)：acksOnFailure(false) 失败任务持续重试忽略 retry_max + 被 cancel 终止验证
  - [ ] SubTask 67.31: max_tasks_per_child (C14)：XHJOB_MAX_TASKS_PER_CHILD=5 daemon 执行 5 个任务后优雅退出验证
  - [ ] SubTask 67.32: timezone (A16)：timezone('America/New_York') cron 任务按纽约时区评估 + 解析失败回退全局验证
  - [ ] SubTask 67.33: events (A17)：xhjob_events 查询任务执行事件流 + TTL 清理验证
  - [ ] SubTask 67.34: coalesce (A18)：coalesce(true) 合并 missed / coalesce(false) 丢弃 missed 行为验证
  - [ ] SubTask 67.35: chain (C15)：xhjob_chain 3 步 ETL 流水线 + 前任务输出作为后任务输入 + 失败中断验证
  - [ ] SubTask 67.36: group (C16)：xhjob_group 3 任务并行批处理 + group_state 完成率验证
  - [ ] SubTask 67.37: max_memory_per_child (C17)：XHJOB_MAX_MEMORY_PER_CHILD=100 daemon 内存超阈值后优雅退出验证

- [x] Task 68: 提交并推送远程主分支（**取代 Task 57**，含 24 个新对齐功能改动：6 项 round 2 + 6 项 round 3 + 6 项 round 4 + 6 项 round 5）
  - [ ] SubTask 68.1: `git status` 核对修改文件清单
  - [ ] SubTask 68.2: `git diff` 审查改动内容（确认无意外改动、无删除用户文件）
  - [ ] SubTask 68.3: `git add <指定文件>` 暂存改动（不 `git add -A`，避免误加 spec 文档外文件）
  - [ ] SubTask 68.4: `git commit -m "feat: 对齐 APScheduler/Celery 35 项（A1-A18+C1-C17）+ maxExecutions bug 修复 + 24 项新对齐功能（every/runAt/jitter/expires/requeue/retryBackoff/maxInstances升级/reschedule/get/ignoreResult/acksLate/softTimeout/misfireGraceTime/replaceExisting/tags/rateLimit/acksOnFailure/maxTasksPerChild/timezone/events/coalesce/chain/group/maxMemoryPerChild）+ 文档/示例/测试补齐"` 提交到本地 main
  - [ ] SubTask 68.5: `git push origin main` 推送到远程主分支
  - [ ] SubTask 68.6: `git log origin/main --oneline -5` 确认远程 HEAD 已更新

# Task Dependencies
- Task 1 独立，最先执行（确认起点干净）
- Task 2 依赖 Task 1（确认起点后编译基线）
- Task 3 依赖 Task 2（编译产物部署）
- Task 4、Task 5、Task 6 可并行（均依赖 Task 2 的编译基线，互不冲突）
- Task 7 依赖 Task 5（next_fire 简化后再叠加 max_executions 逻辑）
- Task 8、Task 9、Task 10、Task 11、Task 12、Task 13 依赖 Task 7（共用 Task struct 与 store trait 扩展，建议串行：先 Task 7 扩展字段，再 Task 8/9/11/12 扩展更多字段，Task 10/13 复用 store trait）
  - 实际执行建议：Task 7 → Task 8（lifecycle）→ Task 9（start/end）→ Task 12（meta）→ Task 11（result_ttl）→ Task 10（list，复用前面字段）→ Task 13（priority，仅队列层改动）
- Task 14（README）、Task 15（examples）可并行（文档与示例，不依赖代码改动；但建议在 Task 7-13 完成后写以保证 API 一致）
- Task 16 依赖 Task 4、6（边界测试包含非法 cron + 4xx 不重试）
- Task 17 依赖 Task 7（maxExecutions 功能完成后测试）
- Task 18 依赖 Task 8（lifecycle API 完成后测试）
- Task 19 依赖 Task 9（start/end 完成后测试）
- Task 20 依赖 Task 11（result_ttl 完成后测试）
- Task 21 依赖 Task 12（meta 完成后测试）
- Task 22、23、24 已被 Task 33、34、35 取代（原范围被扩展，保留作历史记录）
- Task 25 依赖 Task 7（修复 maxExecutions bug，Task 7 引入的 success 路径 bug）
- Task 26（Interval + DateTrigger）依赖 Task 25（共用 Task struct 字段扩展，先修 bug 再加字段）
- Task 27（Jitter）依赖 Task 26（jitter 作用于 cron/interval 任务的 next_fire 计算）
- Task 28（Expires）依赖 Task 26（共用 Task struct 字段，scan_once 改动叠加）
- Task 29（Requeue）依赖 Task 8（Cancelled 终态）+ Task 28（Expired 终态）— 必须先有这两个终态才能 requeue
- Task 30（Retry backoff）依赖 Task 4（retry 模块已修改）
- Task 31（README + examples）依赖 Task 25-30（所有新功能完成后写文档保证一致）
- Task 32（新功能测试）依赖 Task 25-30
- Task 33、34、35 已被 Task 44、45、46 取代（原范围被第三轮扩展，保留作历史记录）
- Task 36（max_instances 升级 A10）依赖 Task 8（OverlapController 已存在，需要其 should_fire 改造）+ Task 25（scan_once 改动叠加在 bug 修复之上）
- Task 37（reschedule A11）依赖 Task 25（scan_once 中 next_fire 计算路径已稳定）+ Task 8（TaskStore trait 已扩展）
- Task 38（get_job A12）依赖 Task 10（load_task 与 TaskSummary 已存在，复用 JSON 序列化）
- Task 39（ignoreResult C9）依赖 Task 25（process_one 改动叠加在 bug 修复之上）+ Task 11（results 表已扩展）
- Task 40（acksLate C10）依赖 Task 39（共用 Task struct 新增 acks_late 字段，避免重复 ALTER TABLE）+ Task 25（daemon_main 改动叠加在 bug 修复之上）
- Task 41（softTimeout C11）依赖 Task 25（executor 路径已稳定）+ Task 39（共用 Task struct 新增 soft_timeout 字段）
- Task 42（README + examples 第三轮）依赖 Task 36-41（所有新功能完成后写文档保证一致）
- Task 43（第三轮测试）依赖 Task 36-41
- Task 44 依赖 Task 4-21 + Task 25-32 + Task 36-43（所有改动完成后重新编译 + 全量测试，含 12 个对齐测试）
- Task 45 依赖 Task 44（编译产物部署后再验证 12 个功能模块）
- Task 46 依赖 Task 44、45（无回归后提交推送，含 12 个新对齐功能改动）
- 实际执行建议（第三轮）：Task 36（max_instances 升级）→ Task 37（reschedule）→ Task 38（get_job）→ Task 39（ignoreResult）→ Task 40（acksLate，复用 Task 39 的字段扩展）→ Task 41（softTimeout，复用 Task 39 的字段扩展）→ Task 42 + Task 43（文档与测试并行）→ Task 44 → Task 45 → Task 46
- Task 44、45、46 已被 Task 55、56、57 取代（原范围被第四轮扩展，保留作历史记录）
- Task 47（A13 misfire_grace_time per-job）依赖 Task 25（scan_once 中 next_fire 计算路径已稳定，misfire 检测叠加在 bug 修复之上）+ Task 26（interval/runAt 任务已实现，新增 per-job 字段需在两者上 warn 忽略）
- Task 48（A14 replace_existing 幂等 dispatch）依赖 Task 8（remove_task 已存在，dispatch 路径中复用）+ Task 25（dispatch 路径已稳定）
- Task 49（A15 tags 作业分组）依赖 Task 10（list_tasks 与 TaskSummary 已存在，复用 trait 签名扩展为 `list_tasks(state_filter, tag_filter)`）
- Task 50（C12 rate_limit 每任务限流）依赖 Task 36（OverlapController::should_fire 已升级，rate_limit 检测可在 scan_once 触发前叠加）+ Task 25（scan_once 改动叠加在 bug 修复之上）
- Task 51（C13 acks_on_failure 失败不放弃）依赖 Task 30（retry 模块已修改，schedule_retry 路径已稳定）+ Task 25（process_one 失败路径已稳定）
- Task 52（C14 worker_max_tasks_per_child daemon 自我回收）依赖 Task 25（daemon_main 已修改，Config 与 run 路径已稳定）
- 实际执行建议（第四轮）：Task 47-52 建议拆分为 2 个 Sub-Agent 串行批次执行，避免文件冲突：
  - 批次 A（数据结构层 + 调度层）：Task 47（misfire_grace_time 字段 + scan_once）→ Task 49（tags 字段 + list_tasks 签名扩展）→ Task 50（rate_limit 字段 + 新模块 rate_limit.rs + scan_once 叠加）
  - 批次 B（执行层 + daemon 层）：Task 48（replace_existing + dispatch 路径）→ Task 51（acks_on_failure + process_one 失败路径）→ Task 52（max_tasks_per_child + daemon_main Config/run）
  - 两批串行执行（批次 A 先，批次 B 后），避免 scan_once / process_one / daemon_main 三大热点的并发修改冲突
- Task 53（README + examples 第四轮）依赖 Task 47-52（所有新功能完成后写文档保证一致）
- Task 54（第四轮测试文件）依赖 Task 47-52
- Task 53、Task 54 可并行（文档与测试不冲突，但都依赖 Task 47-52 完成）
- Task 55 依赖 Task 47-54（所有改动完成后重新编译 + 全量测试，含 18 个对齐测试：6 项 round 2 + 6 项 round 3 + 6 项 round 4）（**已被 Task 66 取代**）
- Task 56 依赖 Task 55（编译产物部署后再验证 18 个功能模块）（**已被 Task 67 取代**）
- Task 57 依赖 Task 55、56（无回归后提交推送，含 18 个新对齐功能改动）（**已被 Task 68 取代**）
- 实际执行建议（第四轮收尾）：Task 53 + Task 54（文档与测试并行）→ Task 55（重新编译 + 全量测试）→ Task 56（功能模块独立验证）→ Task 57（提交推送）
- Task 55、56、57 已被 Task 66、67、68 取代（原范围被第五轮扩展，保留作历史记录）
- Task 58（A16 timezone per-job）依赖 Task 25（scan_once 中 cron 评估路径已稳定）+ Task 47（misfire_grace_time per-job 字段已扩展，timezone 与 grace_time 都作用于 cron 评估）
- Task 59（A17 Event listener API）依赖 Task 25（process_one / scan_once / cancel_task / set_paused 等关键路径已稳定，可在每个路径追加 record_event 调用）+ Task 28（Expired 终态已实现，事件流需要覆盖 Expired 类型）
- Task 60（A18 coalesce 显式 per-job）依赖 Task 47（misfire_grace_time per-job 已实现，coalesce 与 grace_time 配合判定 misfire 行为）+ Task 25（scan_once 中 misfire 检测路径已稳定）
- Task 61（C15 Task chain）依赖 Task 25（process_one 完成路径已稳定，可在完成后追加 chain 推进逻辑）+ Task 12（meta 字段已存在，chain_id 可存入 meta 标识任务归属）
- Task 62（C16 Task group）依赖 Task 25（process_one 完成路径已稳定，可在完成后追加 group 推进逻辑）+ Task 12（meta 字段已存在，group_id 可存入 meta 标识任务归属）
- Task 63（C17 worker_max_memory_per_child）依赖 Task 52（C14 max_tasks_per_child 优雅退出流程已实现，C17 复用同一退出函数）+ Task 25（daemon_main 已修改，Config 与 run 路径已稳定）
- 实际执行建议（第五轮）：Task 58-63 建议拆分为 3 个 Sub-Agent 串行批次执行，避免文件冲突：
  - 批次 A（Task struct 字段扩展 + 调度层）：Task 58（timezone 字段 + scan_once cron 评估）→ Task 60（coalesce 字段确认 + scan_once misfire 行为）
  - 批次 B（事件流 + 复合任务）：Task 59（events 模块 + record_event 集成到多个路径）→ Task 61（chain 模块 + ChainExecutor + chains 表）→ Task 62（group 模块 + GroupWatcher + groups 表）
  - 批次 C（daemon 层 + 跨平台）：Task 63（max_memory_per_child + utils/memory.rs + 跨平台依赖）
  - 三批串行执行（批次 A → 批次 B → 批次 C），避免 scan_once / process_one / daemon_main / TaskStore trait 四大热点的并发修改冲突；批次 B 内部 Task 59 → 61 → 62 串行（共用 TaskStore trait 扩展）
- Task 64（README + examples 第五轮）依赖 Task 58-63（所有新功能完成后写文档保证一致）
- Task 65（第五轮测试文件）依赖 Task 58-63
- Task 64、Task 65 可并行（文档与测试不冲突，但都依赖 Task 58-63 完成）
- Task 66 依赖 Task 58-65（所有改动完成后重新编译 + 全量测试，含 24 个对齐测试：6 项 round 2 + 6 项 round 3 + 6 项 round 4 + 6 项 round 5）
- Task 67 依赖 Task 66（编译产物部署后再验证 24 个功能模块）
- Task 68 依赖 Task 66、67（无回归后提交推送，含 24 个新对齐功能改动）
- 实际执行建议（第五轮收尾）：Task 64 + Task 65（文档与测试并行）→ Task 66（重新编译 + 全量测试）→ Task 67（功能模块独立验证）→ Task 68（提交推送）
